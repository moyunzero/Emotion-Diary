---
phase: 06-governance-baseline-gates
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - scripts/governance/depcruise.cjs
  - scripts/governance/allowlist.knip.json
  - eslint.config.js
  - .planning/phases/06-governance-baseline-gates/06-GATE-RULES.md
autonomous: true
requirements:
  - GOV-02
must_haves:
  truths:
    - "团队能清楚知道当前治理等级是 report/warn/error。"
    - "规则可按条件从 warn 升到 error，而不是拍脑袋切换。"
    - "历史问题不阻塞，但新增越界/循环/未使用导出可被门禁识别。"
  artifacts:
    - path: "scripts/governance/depcruise.cjs"
      provides: "依赖图规则与分级严重度"
    - path: "eslint.config.js"
      provides: "边界规则的 lint 阶段接入"
    - path: ".planning/phases/06-governance-baseline-gates/06-GATE-RULES.md"
      provides: "升级条件、阻断策略、回退策略"
  key_links:
    - from: "06-GATE-RULES.md"
      to: "scripts/governance/depcruise.cjs"
      via: "规则级别定义映射"
      pattern: "report|warn|error"
    - from: "eslint.config.js"
      to: "governance verify command"
      via: "lint 阶段门禁"
      pattern: "boundaries"
---

<objective>
把治理从“可运行”升级到“可渐进收紧且有升级条件”，确保门禁不是一次性强硬落地。

Purpose: 满足 GOV-02，落实 report/warn -> error 的策略与可执行条件。  
Output: 分级规则配置、allowlist 基线策略、门禁升级文档。
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-governance-baseline-gates/06-CONTEXT.md
@.planning/phases/06-governance-baseline-gates/06-RESEARCH.md
@.planning/phases/06-governance-baseline-gates/06-01-PLAN.md
@.planning/phases/06-governance-baseline-gates/06-BASELINE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 落地依赖边界与历史债务基线策略</name>
  <read_first>
    - .planning/phases/06-governance-baseline-gates/06-CONTEXT.md
    - .planning/phases/06-governance-baseline-gates/06-BASELINE.md
    - .planning/phases/06-governance-baseline-gates/06-RESEARCH.md
  </read_first>
  <files>scripts/governance/depcruise.cjs, scripts/governance/allowlist.knip.json</files>
  <action>
    按 D-07 定义“基线快照 + allowlist + 只阻断新增”策略：depcruise 规则初始以 report/warn 为主（D-09），候选 error 项覆盖 D-11（三类：跨层越界 import、新增循环依赖、新增未使用导出），但升级动作必须由文档条件控制（D-10），不能直接全 error。配置范围严格保持 D-06。
  </action>
  <verify>
    <automated>node -e "require('./scripts/governance/depcruise.cjs'); console.log('depcruise config load ok')"</automated>
  </verify>
  <acceptance_criteria>
    - depcruise 规则文件可被 Node 正常加载。
    - allowlist 文件存在并用于历史债务收口策略。
    - 配置中显式体现 report/warn 分级与 D-11 候选项。
  </acceptance_criteria>
  <done>治理规则具备“先可见、再收紧”的技术载体，符合 D-07/D-09/D-10/D-11。</done>
</task>

<task type="auto">
  <name>Task 2: 在 ESLint 与治理文档中同步门禁升级机制</name>
  <read_first>
    - eslint.config.js
    - .planning/phases/06-governance-baseline-gates/06-CONTEXT.md
    - scripts/governance/depcruise.cjs
  </read_first>
  <files>eslint.config.js, .planning/phases/06-governance-baseline-gates/06-GATE-RULES.md</files>
  <action>
    在 eslint 边界规则中接入渐进级别（初始 report/warn，符合 D-09），并在 06-GATE-RULES.md 明确升级条件“连续两轮 PR 新增违规 = 0 才能从 warn 升 error”（D-10），记录默认阻断与应急解锁策略（D-12）。文档中声明每个重构包最低验证门槛 lint + 相关测试 + 关键路径 smoke（D-15）。
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - eslint 配置可运行且引入边界规则，不破坏现有 lint 命令。
    - 06-GATE-RULES.md 明确写出 report/warn/error 级别、切换条件与失败处理策略。
    - 文档明确包含 D-12 与 D-15 的执行要求。
  </acceptance_criteria>
  <done>GOV-02 从规则配置到执行策略形成闭环，团队可按文档稳定升级门禁。</done>
</task>

</tasks>

<verification>
- `npm run lint` 通过，且边界规则配置生效。
- 06-GATE-RULES.md 可直接指导门禁升级，不依赖额外口头说明。
</verification>

<success_criteria>
- 项目内存在可执行的 report/warn/error 渐进门禁实现与升级条件。
- 历史问题不会阻塞当前阶段，但新增问题可被识别并按策略处理。
</success_criteria>

<output>
After completion, create `.planning/phases/06-governance-baseline-gates/06-02-SUMMARY.md`
</output>
