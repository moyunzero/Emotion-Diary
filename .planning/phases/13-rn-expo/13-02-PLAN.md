---
phase: 13-rn-expo
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - .planning/codebase/EXPO-ROUTER.md
  - CONTRIBUTING.md
autonomous: true
requirements: [RN-02]
must_haves:
  truths:
    - "Expo Router 与 app/ 目录约定在独立文档中可查"
    - "CONTRIBUTING 通过链接指向该文档"
  artifacts:
    - path: .planning/codebase/EXPO-ROUTER.md
      provides: "tabs 分组、主要路由文件职责、与 features/components 分工说明"
---

<objective>
Wave 2：**RN-02** — 将 **Expo Router** 与 `app/` 目录约定写入 `.planning/codebase/EXPO-ROUTER.md`，并在 `CONTRIBUTING.md` 中加入 **可发现链接**（贡献者无需猜文件式路由规则）。

Purpose: 满足 ROADMAP 成功标准第 2 条  
Output: `EXPO-ROUTER.md`；`CONTRIBUTING.md` 更新；`yarn typecheck && yarn lint && yarn test:ci` 绿
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-rn-expo/13-RESEARCH.md
@app/_layout.tsx
@app/(tabs)/_layout.tsx
@CONTRIBUTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新建 EXPO-ROUTER.md（RN-02）</name>
  <files>.planning/codebase/EXPO-ROUTER.md</files>
  <read_first>
    app/_layout.tsx
    app/(tabs)/_layout.tsx
    app/(tabs)/index.tsx
    app/profile.tsx
    app/review-export.tsx
    .planning/phases/13-rn-expo/13-RESEARCH.md
  </read_first>
  <action>
在 `.planning/codebase/` **新建** `EXPO-ROUTER.md`（UTF-8），必须包含：
1. 一级标题：`# Expo Router 与 app/ 约定（焚语）`
2. 固定小节 `## 技术事实`：正文 **逐字**包含子串 `expo-router` 与 `typedRoutes`
3. 固定小节 `## 目录与职责`：无序或有序列表 **至少 5 项**，每项包含 `app/` 下的 **真实路径**（从下列集合选用，可增删但须为仓库存在路径）：`app/_layout.tsx`、`app/(tabs)/_layout.tsx`、`app/(tabs)/index.tsx`、`app/(tabs)/record.tsx`、`app/(tabs)/insights.tsx`、`app/profile.tsx`、`app/review-export.tsx`
4. 固定小节 `## 与 features / components`：至少 **两句中文**，说明「路由壳层在 `app/`，可复用 UI 与业务组装主要在 `components/` 与 `features/`」（允许改写但必须保留三个目录名 `app`、`components`、`features` 作为行内代码或路径形式）
5. 固定小节 `## Deep linking`：正文包含 **逐字**子串 `emotiondiary`（与 `app.json` 中 `scheme` 一致）
  </action>
  <verify>
    <automated>test -f .planning/codebase/EXPO-ROUTER.md && rg -q '^# Expo Router 与 app/ 约定' .planning/codebase/EXPO-ROUTER.md && rg -q 'expo-router' .planning/codebase/EXPO-ROUTER.md && rg -q 'typedRoutes' .planning/codebase/EXPO-ROUTER.md && rg -q 'emotiondiary' .planning/codebase/EXPO-ROUTER.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/codebase/EXPO-ROUTER.md` 存在
    - 文件含 `## 目录与职责` 与 `## 与 features / components`
    - 文件含至少 5 处子串 `app/`（列表项中）
    - 文件含 `components` 与 `features` 与 `app`（目录约定段）
  </acceptance_criteria>
  <done>路由约定文档已落盘</done>
</task>

<task type="auto">
  <name>Task 2: CONTRIBUTING 链接到 EXPO-ROUTER.md（RN-02）</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    CONTRIBUTING.md
    .planning/codebase/EXPO-ROUTER.md
  </read_first>
  <action>
编辑 `CONTRIBUTING.md`：在 `## 📝 代码规范` 小节 **内或紧邻之前**，新增一个 **二级标题**，标题必须 **逐字**为 `## 路由与 app 目录（Expo Router）`。该标题下必须包含 Markdown 链接：`[.planning/codebase/EXPO-ROUTER.md](./.planning/codebase/EXPO-ROUTER.md)`（路径与括号内相对路径 **逐字**一致）。
  </action>
  <verify>
    <automated>rg -q '## 路由与 app 目录（Expo Router）' CONTRIBUTING.md && rg -q '\[\.planning/codebase/EXPO-ROUTER\.md\]\(\./\.planning/codebase/EXPO-ROUTER\.md\)' CONTRIBUTING.md</automated>
  </verify>
  <acceptance_criteria>
    - `CONTRIBUTING.md` 含逐字二级标题 `## 路由与 app 目录（Expo Router）`
    - `CONTRIBUTING.md` 含逐字链接 `[.planning/codebase/EXPO-ROUTER.md](./.planning/codebase/EXPO-ROUTER.md)`
  </acceptance_criteria>
  <done>贡献指南已索引路由文档</done>
</task>

</tasks>

<verification_criteria>
- `yarn typecheck` 退出码 0
- `yarn lint` 退出码 0
- `yarn test:ci` 退出码 0
</verification_criteria>
