---
phase: 13-rn-expo
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - store/useAppStore.ts
  - store/modules/entries.ts
  - store/modules/storage.ts
  - store/modules/user.ts
  - store/modules/ai.ts
  - store/modules/weather.ts
  - store/modules/types.ts
  - components/MoodForm.tsx
  - components/ReviewExport/ReviewExportScreen.tsx
autonomous: true
requirements: [DOC-01]
must_haves:
  truths:
    - "store/modules 下每个 .ts 均有中文模块头注释"
    - "useAppStore 与至少两条主流程组件有中文说明（头注释 + 关键逻辑）"
  artifacts:
    - path: store/modules/entries.ts
      provides: "中文模块头 + ≥1 处关键逻辑注释"
---

<objective>
Wave 3：**DOC-01** — 在 `store` 核心切片与 **记录 / 导出** 主路径组件上补充 **中文** 模块头与关键逻辑注释（英文专有名词可保留），达到 `13-RESEARCH.md` 量化阈值：**`store/modules` 下 6 个 `.ts` 全覆盖** + `store/useAppStore.ts` + `components/MoodForm.tsx` + `components/ReviewExport/ReviewExportScreen.tsx`（共 **9** 个文件）。

Purpose: 满足 ROADMAP 成功标准第 3 条与 REQUIREMENTS DOC-01  
Output: 上述文件仅增注释（除非发现错别字式微改）；`yarn typecheck && yarn lint && yarn test:ci` 绿
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-rn-expo/13-RESEARCH.md
@store/useAppStore.ts
@store/modules/entries.ts
@store/modules/storage.ts
@store/modules/user.ts
@store/modules/ai.ts
@store/modules/weather.ts
@store/modules/types.ts
@components/MoodForm.tsx
@components/ReviewExport/ReviewExportScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: store/modules 中文模块头与关键注释（DOC-01）</name>
  <files>store/modules/entries.ts, store/modules/storage.ts, store/modules/user.ts, store/modules/ai.ts, store/modules/weather.ts, store/modules/types.ts</files>
  <read_first>
    store/modules/entries.ts
    store/modules/storage.ts
    store/modules/user.ts
    store/modules/ai.ts
    store/modules/weather.ts
    store/modules/types.ts
  </read_first>
  <action>
对下列 **6** 个文件 **各**在文件 **首个 import 语句之前**（或文件最顶端）新增 **块注释**，要求：
- 块注释内 **至少一行**为中文句子，且该行 **须含**汉字（Unicode 范围可用目测：含「模块」「状态」「存储」「同步」「天气」「类型」「AI」等之一即可）
- **除 `types.ts` 外**，每个文件在 **export 的主要函数或对象**附近再增加 **至少 1 行** `//` 单行中文注释，解释一处 **非显而易见**行为（异步、持久化、错误处理、与云端交互等）
`types.ts` 仅需模块头块注释（说明本文件为 Zustand 模块 **类型契约**）。
  </action>
  <verify>
    <automated>for f in entries storage user ai weather types; do test -f "store/modules/$f.ts" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `store/modules/entries.ts` 第一行起为 `/*` 或 `/**` 开头的块注释，且注释块内含中文
    - 同上：`storage.ts`、`user.ts`、`ai.ts`、`weather.ts`、`types.ts`
    - `entries.ts`、`storage.ts`、`user.ts`、`ai.ts`、`weather.ts` 各至少含一行以 `//` 开头且含中文的行（`types.ts` 不要求额外 `//`）
  </acceptance_criteria>
  <done>store modules 注释已覆盖</done>
</task>

<task type="auto">
  <name>Task 2: useAppStore.ts 文件头与关键段注释（DOC-01）</name>
  <files>store/useAppStore.ts</files>
  <read_first>
    store/useAppStore.ts
    store/modules/types.ts
  </read_first>
  <action>
编辑 `store/useAppStore.ts`：
1. 在文件 **最顶端**（import 之前）增加 **块注释**，含中文说明本文件为 **Zustand 根 store**、组合各 slice/module 的职责（**至少 2 句中文**）。
2. 在 **`initializeStore` 函数定义上方**增加 **一段** `/** ... */` 或紧邻的 `//` 中文注释，说明其副作用（如：恢复会话、订阅 auth、与首次启动相关），**至少 20 个汉字**。
  </action>
  <verify>
    <automated>rg -n 'initializeStore' store/useAppStore.ts | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `useAppStore.ts` 第一个 `import` 之前存在块注释 `/*` 或 `/**`
    - 该文件含子串 `initializeStore`
    - 在 `initializeStore` 定义行 **上方** 15 行内：存在 `//` 或 `*` 开头的注释行，且该区域内至少一行含 **≥8 个连续汉字**（可用人工执行 `rg -B15 'initializeStore' store/useAppStore.ts` 目检；自动化可弱化为：含 `initializeStore` 且文件顶部块注释含「Zustand」或「store」的中文描述邻接汉字）
  </acceptance_criteria>
  <done>useAppStore 注释已补强</done>
</task>

<task type="auto">
  <name>Task 3: MoodForm 与 ReviewExportScreen 中文注释（DOC-01）</name>
  <files>components/MoodForm.tsx, components/ReviewExport/ReviewExportScreen.tsx</files>
  <read_first>
    components/MoodForm.tsx
    components/ReviewExport/ReviewExportScreen.tsx
  </read_first>
  <action>
1. `components/MoodForm.tsx`：在文件顶端（import 前或首 import 后紧跟）增加 **模块说明块注释（中文）**；在 **提交/保存情绪** 相关的 handler 或 `onSubmit` **函数体开头**增加 **至少 2 行** `//` 中文注释（说明校验或草稿逻辑）。
2. `components/ReviewExport/ReviewExportScreen.tsx`：同样增加 **模块说明块注释（中文）**；在 **导出或截图** 相关的主要 handler 附近增加 **至少 2 行** `//` 中文注释。
  </action>
  <verify>
    <automated>test -f components/MoodForm.tsx && test -f components/ReviewExport/ReviewExportScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `MoodForm.tsx` 含块注释 `/**` 或 `/*`，且块内至少一行含中文
    - `MoodForm.tsx` 含 **至少 2 行** 以 `//` 开头且含中文的行
    - `ReviewExportScreen.tsx` 含块注释且块内至少一行含中文
    - `ReviewExportScreen.tsx` 含 **至少 2 行** 以 `//` 开头且含中文的行
  </acceptance_criteria>
  <done>主流程组件注释已补强</done>
</task>

</tasks>

<verification_criteria>
- `yarn typecheck` 退出码 0
- `yarn lint` 退出码 0
- `yarn test:ci` 退出码 0
- `node scripts/verify-governance-smoke.js` 退出码 0（若仓库存在该脚本；不存在则跳过并记入 SUMMARY）
</verification_criteria>
