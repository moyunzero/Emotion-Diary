---
phase: 13-rn-expo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/codebase/EXPO-RN-AUDIT.md
  - .planning/codebase/STACK.md
autonomous: true
requirements: [RN-01]
must_haves:
  truths:
    - "app.json / babel / metro / tsconfig 核对结论以表格落盘并可从 STACK 索引"
    - "刻意偏差（newArch、typedRoutes、reactCompiler、drop_console）有书面说明"
  artifacts:
    - path: .planning/codebase/EXPO-RN-AUDIT.md
      provides: "配置审计表 + 版本锚点 + 参考链接占位"
---

<objective>
Wave 1：**RN-01** — 将 `app.json`、`babel.config.js`、`metro.config.js`、`tsconfig.json`（及与 Expo 54 直接相关的 `package.json` 脚本入口字段）与当前 **Expo SDK 54** 实践对齐结论 **结构化落盘**，并在 `.planning/codebase/STACK.md` 增加 **指向审计文档** 的索引行。

Purpose: 满足 ROADMAP 成功标准第 1 条  
Output: `EXPO-RN-AUDIT.md`；`STACK.md` 更新；`yarn typecheck && yarn lint && yarn test:ci` 绿
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-rn-expo/13-RESEARCH.md
@app.json
@babel.config.js
@metro.config.js
@tsconfig.json
@package.json
@.planning/codebase/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新建 EXPO-RN-AUDIT.md（RN-01）</name>
  <files>.planning/codebase/EXPO-RN-AUDIT.md</files>
  <read_first>
    .planning/phases/13-rn-expo/13-RESEARCH.md
    app.json
    babel.config.js
    metro.config.js
    tsconfig.json
    package.json
  </read_first>
  <action>
在 `.planning/codebase/` **新建** `EXPO-RN-AUDIT.md`（UTF-8），必须包含：
1. 一级标题：`# Expo / React Native 配置审计（焚语）`
2. 固定小节 `## 版本锚点`：正文写出 **逐字**子串 `expo ~54`、`react-native 0.81`（数字可随 package.json 更新）、`expo-router`
3. 固定小节 `## 配置核对表`：Markdown **表格**，列名必须含：**文件**、**关注点**、**当前值摘要**、**结论**、**备注**；表格 **至少 4 行数据**，分别对应 `app.json`、`babel.config.js`、`metro.config.js`、`tsconfig.json`（关注点示例：`newArchEnabled`、`reanimated 插件顺序`、`drop_console`、`strict`+paths）
4. 固定小节 `## 刻意偏差与说明`：至少 **一条** 关于 `metro.config.js` 中 `drop_console: true` 的说明（生产剥离 console，与本地开发使用 `expo start` 的关系）
5. 固定小节 `## 参考`：至少一个指向 `https://docs.expo.dev/` 的 Markdown 链接
  </action>
  <verify>
    <automated>test -f .planning/codebase/EXPO-RN-AUDIT.md && rg -q '^# Expo / React Native 配置审计' .planning/codebase/EXPO-RN-AUDIT.md && rg -q '## 版本锚点' .planning/codebase/EXPO-RN-AUDIT.md && rg -q '## 配置核对表' .planning/codebase/EXPO-RN-AUDIT.md && rg -q '## 刻意偏差与说明' .planning/codebase/EXPO-RN-AUDIT.md && rg -q 'https://docs.expo.dev/' .planning/codebase/EXPO-RN-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - 文件 `.planning/codebase/EXPO-RN-AUDIT.md` 存在
    - 该文件含子串 `expo ~54` 与 `react-native 0.81`
    - 该文件含表格分隔行 `|---|`（至少一处）
    - 该文件含子串 `drop_console`
    - 该文件含 `https://docs.expo.dev/` 的 Markdown 链接语法
  </acceptance_criteria>
  <done>RN 配置审计已落盘</done>
</task>

<task type="auto">
  <name>Task 2: STACK.md 增加审计文档索引（RN-01）</name>
  <files>.planning/codebase/STACK.md</files>
  <read_first>
    .planning/codebase/STACK.md
    .planning/codebase/EXPO-RN-AUDIT.md
  </read_first>
  <action>
编辑 `.planning/codebase/STACK.md`：在 **概述** 段落后、`## 语言与运行时` **之前**，插入一个新小节，**二级标题** 必须为 `## 配置审计（Phase 13）`。该小节内必须包含 Markdown 链接，**括号内相对路径逐字**为 `./EXPO-RN-AUDIT.md`，链接文字含子串 `EXPO-RN-AUDIT` 或「配置审计」其一即可。
  </action>
  <verify>
    <automated>rg -q '## 配置审计（Phase 13）' .planning/codebase/STACK.md && rg -q '\(\./EXPO-RN-AUDIT\.md\)' .planning/codebase/STACK.md</automated>
  </verify>
  <acceptance_criteria>
    - `STACK.md` 含逐字行或子串 `## 配置审计（Phase 13）`
    - `STACK.md` 含逐字子串 `(./EXPO-RN-AUDIT.md)`（Markdown 链接路径）
  </acceptance_criteria>
  <done>STACK 已链到审计文档</done>
</task>

</tasks>

<verification_criteria>
- `yarn typecheck` 退出码 0
- `yarn lint` 退出码 0
- `yarn test:ci` 退出码 0
</verification_criteria>
