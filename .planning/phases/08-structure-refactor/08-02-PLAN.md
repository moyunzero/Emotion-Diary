---
phase: 08-structure-refactor
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - store/useAppStore.ts
  - store/modules/entries.ts
  - store/modules/types.ts
autonomous: true
requirements:
  - ARC-02
must_haves:
  truths:
    - "`useAppStore.getState()` 与现有 selector（如 `useAppStore((s) => s.entries)`、`addEntry`、`updateEntry`）对外形状不变（D-10）。"
    - "`_loadEntries` 与 `_saveEntries` 仅有一处实现，entries 持久化（迁移、loadFromStorage、防抖 saveToStorage）与拆分前行为一致。"
    - "entries 域以 Zustand v5 Slices Pattern 组合：`create<AppStore>()((...a) => ({ ...createEntriesSlice(...a), ... }))` 或等价展开，类型使用 `StateCreator<AppStore, [], [], EntriesSlice>`（D-11、08-RESEARCH）。"
  artifacts:
    - path: "store/modules/entries.ts"
      provides: "完整 entries slice：CRUD + 真实 _loadEntries/_saveEntries + 模块级防抖 ref"
    - path: "store/useAppStore.ts"
      provides: "create 体中移除与 entries 持久化重复的键；cleanupStoreTimers 仍清除 entries 防抖定时器"
    - path: "store/modules/types.ts"
      provides: "Entries 相关类型与 ModuleCreator/StateCreator 交叉类型导出（若需）"
  key_links:
    - from: "store/modules/entries.ts"
      to: "store/modules/storage"
      via: "migrateFromLegacyStorage、loadFromStorage、saveToStorage、getStorageKey"
      pattern: "loadFromStorage|saveToStorage|migrateFromLegacy"
    - from: "store/modules/entries.ts"
      to: "store/useAppStore.ts"
      via: "get()._calculateWeather 等跨 slice 调用保持不变"
      pattern: "_calculateWeather"
    - from: "store/useAppStore.ts"
      to: "cleanupStoreTimers"
      via: "清除 saveEntriesTimeoutRef（ref 随 _saveEntries 迁移后须仍被清理）"
      pattern: "cleanupStoreTimers|saveEntriesTimeoutRef"
---

<objective>
完成 store 首批 **entries** slice：将 `useAppStore.ts` 中的 `_loadEntries`/`_saveEntries` 与模块级防抖收拢到 entries 模块，消除与 `createEntriesModule` 占位重复实现，满足 ARC-02 与 D-09～D-12。

Purpose: 单一职责、避免 spread 静默覆盖（08-RESEARCH Pitfall 1）。  
Output: entries 切片内聚、对外 API 不变。
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-structure-refactor/08-CONTEXT.md
@.planning/phases/08-structure-refactor/08-RESEARCH.md
@store/useAppStore.ts（`create` 返回体、`saveEntriesTimeoutRef`、`_loadEntries`/`_saveEntries` 行块）
@store/modules/entries.ts（当前占位 `_loadEntries`/`_saveEntries`）
@store/modules/storage.ts（getStorageKey、loadFromStorage、saveToStorage、migrateFromLegacyStorage）
@store/modules/types.ts（EntriesModule、AppStore、ModuleCreator）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 将 _loadEntries/_saveEntries 与防抖 ref 迁入 entries 模块并删除主文件重复实现</name>
  <read_first>
    - store/useAppStore.ts（全文检索 `_loadEntries`、`_saveEntries`、`saveEntriesTimeoutRef`、文件顶部 import）
    - store/modules/entries.ts
    - store/modules/storage.ts
    - .planning/phases/08-structure-refactor/08-RESEARCH.md（Zustand Slices Pattern、Pitfall 1）
  </read_first>
  <files>store/modules/entries.ts, store/useAppStore.ts</files>
  <action>
    1. 在 `store/modules/entries.ts` 顶部增加与 `useAppStore.ts` 当前 `_loadEntries`/`_saveEntries` **逐行等价**的实现所需 import（`migrateFromLegacyStorage`、`loadFromStorage`、`saveToStorage`、`getStorageKey` 等）；**不要**改变 storage key、防抖 **500ms** 常量、或 set/get 调用顺序。
    2. 将 **`saveEntriesTimeoutRef` 模块变量**从 `useAppStore.ts` 移到 `entries.ts`（或单独的 `store/modules/entriesPersistence.ts` 再由 entries import），保证全应用仍只有一个定时器实例；`cleanupStoreTimers` 必须仍能清除该 ref——做法：`entries.ts` 导出 `clearEntriesSaveDebounce()` 或在 `cleanupStoreTimers` 内调用 entries 模块导出的清理函数（禁止留下无法清理的悬挂定时器）。
    3. 在 `createEntriesModule` 内用真实函数**替换**占位 `_loadEntries`/`_saveEntries`，实现体从 `useAppStore.ts` **原样搬迁**（含 `get()._calculateWeather()` 调用）。
    4. 从 `useAppStore.ts` 的 `return { ... }` 中**删除** `_loadEntries`、`_saveEntries` 键以及已迁移的 `saveEntriesTimeoutRef` 定义；合并后运行 `rg "_loadEntries|_saveEntries" store/` 确认**仅 entries 模块**包含实现（除类型声明与调用点外）。
    5. 保持 `...entriesModule` 仍在 `create` 返回对象中且**不要被**后续同名键覆盖；若当前顺序为 spread 在前、覆盖在后，删除覆盖后 entries 方法必须仍暴露到 `AppStore`。
  </action>
  <verify>
    <automated>cd /Users/moyun/code/Emotion-Diary && yarn lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg` 显示 `_loadEntries`/`_saveEntries` 的实现只在 `store/modules/entries.ts`（`useAppStore.ts` 内无重复 function 体）。
    - `cleanupStoreTimers` 仍可清理 entries 保存防抖定时器。
    - `yarn lint` 退出码 0。
  </acceptance_criteria>
  <done>entries 持久化逻辑单点维护，无 spread 双实现（D-12、08-RESEARCH）。</done>
</task>

<task type="auto">
  <name>Task 2: 显式 Slices Pattern 组合与 StateCreator 交叉类型</name>
  <read_first>
    - store/useAppStore.ts（create 工厂整体结构）
    - store/modules/types.ts
    - store/modules/weather.ts、store/modules/ai.ts（spread 方式，避免破坏合并顺序）
  </read_first>
  <files>store/useAppStore.ts, store/modules/entries.ts, store/modules/types.ts</files>
  <action>
    1. 将 `createEntriesModule` 重命名为 `createEntriesSlice`（或保留原名但添加别名导出），并将其标注为 `StateCreator<AppStore, [], [], Pick<AppStore, EntriesKeys>>` 或项目内等价的 `EntriesModule` 扩展类型（遵循 zustand v5 advanced-typescript slices 示例）；**目标**是 `get()` 在 slice 内能类型安全访问 `user`、`_calculateWeather` 等 `AppStore` 其余字段（08-RESEARCH）。
    2. 将 `useAppStore` 的 `create<AppStore>((set, get) => { ... })` 改为推荐形式：`create<AppStore>()((...args) => ({ ...createEntriesSlice(...args), ...createWeatherModule(...args), ... }))` **仅当**与现有 `weather`/`ai` 工厂签名兼容；若 `createWeatherModule` 仍为 `(set,get)=>object` 而非 variadic，则采用过渡方案：`const a = args as const; const set = a[0]; const get = a[1];` 包装调用，**禁止**改变 weather/ai 返回值内容。
    3. 更新 `store/modules/types.ts`：导出 slice 类型、保证 `AppStore` interface 仍包含 `entries`、`_loadEntries`、`_saveEntries` 等键；对外 `useAppStore` 的 TypeScript 类型与 `getState()` 自动补全与重构前一致（D-10）。
    4. 全仓 `rg "createEntriesModule"` 更新为新的导出名（若有重命名）。
  </action>
  <verify>
    <automated>cd /Users/moyun/code/Emotion-Diary && yarn lint && yarn test:ci</automated>
  </verify>
  <acceptance_criteria>
    - TypeScript 严格模式无新增 `any` 掩盖 entries slice。
    - `yarn lint` 与 `yarn test:ci` 退出码 0。
    - `useAppStore.getState().entries` 与 `_loadEntries` 在运行时可调用（无 undefined 方法）。
  </acceptance_criteria>
  <done>ARC-02：首批 entries slice 范式落地，对外 API 不变（D-10、D-11）。</done>
</task>

</tasks>

<verification>
- `yarn test:ci` 绿。
- 手测：冷启动加载 entries、增删改条目后杀进程重进数据仍在；同步流不报错（08-RESEARCH 验证维度）。
</verification>

<success_criteria>
- entries 读写与持久化路径单一职责；无静默覆盖。
- 外部调用方式与重构前一致。
</success_criteria>

<output>
After completion, create `.planning/phases/08-structure-refactor/08-02-SUMMARY.md`
</output>
