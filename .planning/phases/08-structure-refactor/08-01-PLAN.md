---
phase: 08-structure-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/profile.tsx
  - package.json
  - features/profile/ProfileScreen.tsx
  - features/profile/index.ts
  - features/profile/components/ProfileHeaderSection.tsx
  - features/profile/components/ProfileStatsSection.tsx
  - features/profile/components/ProfileSettingsSection.tsx
  - features/profile/hooks/useProfileScreenState.ts
  - features/profile/hooks/useProfileAuthHandlers.ts
  - features/profile/hooks/useProfileSyncHandlers.ts
  - features/profile/styles/profileScreen.styles.ts
autonomous: true
requirements:
  - ARC-01
must_haves:
  truths:
    - "app/profile.tsx 仅承担 Expo 路由默认导出与最外层布局容器，行数相对拆分前显著减少。"
    - "个人页未登录/已登录展示、统计区、同步与账号菜单、登录注册与编辑资料 Modal 行为与拆分前一致。"
    - "profile 业务代码集中在 features/profile/，hooks 位于 features/profile/hooks/（D-05、D-06、D-08）。"
  artifacts:
    - path: "app/profile.tsx"
      provides: "路由壳层：默认导出包裹 ProfileScreen 或等价一层 layout"
    - path: "features/profile/ProfileScreen.tsx"
      provides: "页面组装：组合 Header/Stats/Settings 三区与 Modal"
    - path: "features/profile/components/ProfileHeaderSection.tsx"
      provides: "头部区：沿用 components/Profile 的 ProfileHeader、ProfileUserCard 等，无行为变更"
    - path: "features/profile/components/ProfileStatsSection.tsx"
      provides: "统计区：ProfileStatCard、CompanionDaysCard 等与拆分前一致"
    - path: "features/profile/components/ProfileSettingsSection.tsx"
      provides: "设置与数据区：同步入口、恢复、注销、菜单项与相关 Modal 触发"
    - path: "features/profile/hooks/useProfileScreenState.ts"
      provides: "Modal 开关、表单本地 state、Toast、键盘与滚动 ref 等 UI 状态"
    - path: "features/profile/hooks/useProfileAuthHandlers.ts"
      provides: "handleAuthSubmit、handleSaveProfile、注册切换等与 store 的调用链"
    - path: "features/profile/hooks/useProfileSyncHandlers.ts"
      provides: "syncToCloud、recoverFromCloud、isSyncingRef 防抖等同步流程"
  key_links:
    - from: "features/profile/ProfileScreen.tsx"
      to: "../store/useAppStore"
      via: "useAppStore selectors 与 login/logout/sync 等方法"
      pattern: "useAppStore"
    - from: "features/profile/hooks/useProfileAuthHandlers.ts"
      to: "../store/useAppStore"
      via: "login、register、updateUser、deleteAccount"
      pattern: "useAppStore\\(\\(state\\)"
    - from: "app/profile.tsx"
      to: "@/features/profile/ProfileScreen"
      via: "默认导出组件仅渲染 ProfileScreen"
      pattern: "ProfileScreen"
---

<objective>
将 `app/profile.tsx` 壳层化：路由文件只保留默认导出与一层 layout，页面主体与 Modal/handler 下沉到 `features/profile/`，满足 ARC-01 与 D-01～D-08。

Purpose: 降低 ~1448 行单文件复杂度，不改动用户可见行为。  
Output: `features/profile/` 目录、三区 UI 子区块、专用 hooks、`app/profile.tsx` 薄壳。
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-structure-refactor/08-CONTEXT.md
@.planning/phases/08-structure-refactor/08-RESEARCH.md
@tsconfig.json（paths `@/*` 已存在，profile 壳层使用 `@/features/profile/...` 导入）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 features/profile 壳层并让路由文件仅 re-export</name>
  <read_first>
    - app/profile.tsx（全文，记录 import、state、handler、JSX 根结构）
    - .planning/phases/08-structure-refactor/08-CONTEXT.md（D-05～D-08）
    - tsconfig.json（确认 `@/*` → 项目根）
    - package.json（scripts.lint 当前 glob 列表）
  </read_first>
  <files>app/profile.tsx, features/profile/ProfileScreen.tsx, features/profile/index.ts, package.json</files>
  <action>
    1. 新建 `features/profile/ProfileScreen.tsx`：将当前 `app/profile.tsx` 中除默认导出命名外的**全部实现**（import、常量、组件体、样式若仍在文件内则暂可整块迁入）移入该文件；组件名改为 `ProfileScreen` 并 `export function ProfileScreen`（或 named export）。`export default` 不要放在此文件，以便路由壳层独占默认导出（D-06）。
    2. 新建 `features/profile/index.ts`，`export { ProfileScreen } from './ProfileScreen'`（可选再 export 类型）。
    3. 将 `app/profile.tsx` 替换为：仅 `import { ProfileScreen } from '@/features/profile'`（或 `@/features/profile/ProfileScreen`）与 `export default function ProfileRoute() { return <ProfileScreen />; }`；若需 SafeAreaProvider 等最外层容器且原文件没有则不要新增，避免行为变化。
    4. **必须**：更新 `package.json` 的 `lint` 脚本，在现有目录列表中加入 `features`，使 `features/profile` 纳入 ESLint，否则治理与本地检查会漏扫新代码。
    5. 修正所有因文件位移产生的相对路径：优先使用 `@/components/...`、`@/store/...`、`@/styles/...`、`@/utils/...`；禁止从 `app/profile.tsx` 再引用已迁移逻辑。
    6. 遇循环依赖（features ↔ store ↔ components）时按 D-03 停步：调整 import 方向（数据自上而下）后再继续，不跳过。
  </action>
  <verify>
    <automated>cd /Users/moyun/code/Emotion-Diary && yarn lint</automated>
  </verify>
  <acceptance_criteria>
    - `app/profile.tsx` 仅含壳层（默认导出 + 单行或少量 JSX），无业务 Modal 实现残留。
    - `yarn lint` 退出码 0，且 lint 命令包含 `features` 目录。
    - Metro/TS 能解析 `@/features/profile`（无 unresolved import）。
  </acceptance_criteria>
  <done>Profile 页面行为与拆分前一致（编译+lint 通过），路由文件已薄壳化（D-01 第一步、D-06）。</done>
</task>

<task type="auto">
  <name>Task 2: 拆分为 ProfileHeaderSection / ProfileStatsSection / ProfileSettingsSection 并下沉 hooks</name>
  <read_first>
    - features/profile/ProfileScreen.tsx（Task 1 结果）
    - styles/components/Profile.styles.ts（沿用 profileContentPadding、profileStyles 等）
    - components/Profile/index 导出（ProfileHeader、ProfileUserCard、ProfileStatCard 等）
  </read_first>
  <files>features/profile/ProfileScreen.tsx, features/profile/components/ProfileHeaderSection.tsx, features/profile/components/ProfileStatsSection.tsx, features/profile/components/ProfileSettingsSection.tsx, features/profile/hooks/useProfileScreenState.ts, features/profile/hooks/useProfileAuthHandlers.ts, features/profile/hooks/useProfileSyncHandlers.ts, features/profile/styles/profileScreen.styles.ts</files>
  <action>
    1. 按 D-07 将主渲染拆为三个子区块组件（命名固定为 `ProfileHeaderSection`、`ProfileStatsSection`、`ProfileSettingsSection`，文件放在 `features/profile/components/`）：
       - **ProfileHeaderSection**：`ScreenContainer` 内上半部分——`ProfileHeader`、`ProfileUserCard` 及与之直接相关的 props（如 insets、router 若仅头部需要可留在 Screen 传入）。
       - **ProfileStatsSection**：统计卡片与 `CompanionDaysCard`、`CompanionDaysModal` 的展示与打开入口（若 Modal 主体过大可仍由 Screen 持有 Modal 树，但触发与数据 props 需清晰）。
       - **ProfileSettingsSection**：`ProfileSectionHeader`、同步/恢复/注销等 `ProfileMenuItem`、进度指示、以及登录/注册/编辑资料 **Modal 的 JSX 树**（保持原 Modal `visible`/`onRequestClose`/表单结构与 `Alert` 文案不变）。
    2. 按 D-08 将本地 state 与 handler 从 `ProfileScreen.tsx` 抽到 `features/profile/hooks/`：
       - `useProfileScreenState.ts`：`useState`/`useRef`（登录/注册/编辑/CompanionDays Modal、loading、Toast、keyboard、modal ScrollView ref 等），返回只读 state 与 setter 包。
       - `useProfileAuthHandlers.ts`：依赖 `useAppStore` 的 `login`/`register`/`logout`/`deleteAccount`/`updateUser` 等与认证、资料保存相关的 async 函数，**逻辑从原 profile 复制不改写业务分支**。
       - `useProfileSyncHandlers.ts`：`syncToCloud`、`recoverFromCloud`、`isSyncingRef`、重复触发防护等与同步相关逻辑。
    3. 将 `ProfileScreen.tsx` 内联 `StyleSheet.create` 大块迁到 `features/profile/styles/profileScreen.styles.ts` 并在各区块或 Screen 引用；数值、颜色与原文件逐段对照，避免视觉漂移。
    4. `ProfileScreen.tsx` 仅：组合 hooks、向三区传 props、保留 `useEffect` 等与页面生命周期强绑定的副作用（若有）；**不得**删减用户可见流程（注册切换、忘记密码入口若存在须保留）。
  </action>
  <verify>
    <automated>cd /Users/moyun/code/Emotion-Diary && yarn lint && yarn test:ci</automated>
  </verify>
  <acceptance_criteria>
    - 三区组件职责清晰，无互相 import 循环；hooks 不 import 子区块组件（单向：Screen → sections → 展示组件）。
    - `ProfileScreen.tsx` 行数较 Task 1 后显著下降；样式不在 Screen 内联大段 duplicate。
    - `yarn lint` 与 `yarn test:ci` 均退出码 0。
  </acceptance_criteria>
  <done>ARC-01 结构目标达成：路由壳 + feature 模块 + 三区 UI + hooks 落位；自动化检查通过。</done>
</task>

</tasks>

<verification>
- `yarn lint`、`yarn test:ci` 通过。
- 手测（见 08-RESEARCH.md）：打开个人页、登录/注册、编辑资料、同步与恢复入口、陪伴天数弹窗与拆分前一致。
</verification>

<success_criteria>
- `app/profile.tsx` 为薄壳；业务在 `features/profile/`。
- 用户可见流程与文案无 intentional 变更。
</success_criteria>

<output>
After completion, create `.planning/phases/08-structure-refactor/08-01-SUMMARY.md`
</output>
