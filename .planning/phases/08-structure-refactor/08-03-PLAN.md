---
phase: 08-structure-refactor
plan: 03
type: execute
wave: 3
depends_on:
  - 08-02
files_modified:
  - components/EditEntryModal.tsx
  - components/EditEntryModal/EditEntryModal.tsx
  - components/EditEntryModal/EditEntryForm.tsx
  - components/EditEntryModal/EditEntryFields.tsx
  - components/EditEntryModal/editEntryUtils.ts
  - components/EditEntryModal/EditEntryModal.styles.ts
  - components/EditEntryModal/index.ts
  - components/entries/index.ts
  - components/EntryCard.tsx
  - components/types.ts
  - types/components.ts
autonomous: true
requirements:
  - ARC-03
must_haves:
  truths:
    - "`EditEntryModal` 以 `components/EditEntryModal/` 为目录入口，壳层 + 表单逻辑 + 字段视图 + 纯函数 + 样式文件分离（D-14、D-15）。"
    - "对外 props：`entry`、`visible`、`onClose`、`onSuccess` 与默认导出语义保持不变；`React.memo` 比较行为不弱化。"
    - "至少一处调用方改为 `import { EditEntryModal } from '@/components/entries'`（D-16）；旧路径在 Phase 8 可保留兼容 re-export，不强制全仓迁移。"
  artifacts:
    - path: "components/EditEntryModal/EditEntryModal.tsx"
      provides: "壳：Modal、KeyboardAvoidingView、ScrollView、safe area、组合子组件、memo 默认导出"
    - path: "components/EditEntryModal/EditEntryForm.tsx"
      provides: "逻辑：useState/useEffect 同步 entry、handleSubmit、updateEntry、haptic、自定义标签 load/add/remove"
    - path: "components/EditEntryModal/EditEntryFields.tsx"
      provides: "视图：MOOD_CONFIG、deadline、人/触发器、标签 chips、AddTagInput 等纯展示与受控输入"
    - path: "components/EditEntryModal/editEntryUtils.ts"
      provides: "纯函数：toggleSelection、deadline 归一、与 React 无关的计算"
    - path: "components/EditEntryModal/EditEntryModal.styles.ts"
      provides: "StyleSheet.create 从单文件迁出"
    - path: "components/EditEntryModal/index.ts"
      provides: "barrel：默认导出兼容 `import EditEntryModal from '.../EditEntryModal'`"
    - path: "components/entries/index.ts"
      provides: "命名导出 EditEntryModal（D-16）"
  key_links:
    - from: "components/EditEntryModal/EditEntryForm.tsx"
      to: "store/useAppStore"
      via: "updateEntry"
      pattern: "updateEntry"
    - from: "components/EntryCard.tsx"
      to: "@/components/entries"
      via: "D-16 至少一处迁移 import"
      pattern: "@/components/entries"
---

<objective>
将 `components/EditEntryModal.tsx`（~595 行）目录化并按视图/逻辑/纯函数拆分；建立 `@/components/entries` 出口并完成至少一处 import 迁移，满足 ARC-03 与 D-13～D-16。

Purpose: Phase 8 唯一 500+ 组件拆分，降低单文件复杂度且不改变编辑流。  
Output: `components/EditEntryModal/` 包、`components/entries` barrel、EntryCard 新 import。
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-structure-refactor/08-CONTEXT.md
@.planning/phases/08-structure-refactor/08-RESEARCH.md
@components/EditEntryModal.tsx（当前单文件全文）
@components/EntryCard.tsx（调用方）
@components/types.ts 与 types/components.ts（若存在重复 EditEntryModalProps，合并或 re-export 避免漂移）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建目录、抽纯函数与样式，壳层 EditEntryModal 组合子树</name>
  <read_first>
    - components/EditEntryModal.tsx（全文，标记：Modal 壳、state、useEffect、handleSubmit、StyleSheet、模块级 toggleSelection）
    - .planning/phases/08-structure-refactor/08-RESEARCH.md（EditEntryModal 拆分边界表）
  </read_first>
  <files>components/EditEntryModal.tsx, components/EditEntryModal/editEntryUtils.ts, components/EditEntryModal/EditEntryModal.styles.ts, components/EditEntryModal/EditEntryModal.tsx, components/EditEntryModal/index.ts</files>
  <action>
    1. 新建目录 `components/EditEntryModal/`。将现有 `components/EditEntryModal.tsx` **移动**为目录内文件或拆块：删除仓库根下**单文件** `components/EditEntryModal.tsx`（避免 Metro 同名校验歧义），以 `components/EditEntryModal/index.ts` 或 `EditEntryModal.tsx` 作为包入口（D-14）。
    2. 创建 `editEntryUtils.ts`：迁入与 React 无关的纯函数（至少包含当前文件中的 `toggleSelection` 及 deadline 归一相关逻辑）；**禁止**在 utils 内调用 hooks 或 `useAppStore`。
    3. 创建 `EditEntryModal.styles.ts`：`StyleSheet.create(...)` 整段迁入，导出样式对象；类名键名保持不变以便 diff 最小。
    4. 创建 `EditEntryModal.tsx`（壳层）：保留 `Modal`、`visible`、`onClose`、`KeyboardAvoidingView`、`ScrollView` ref、安全区；**暂时**可内联 `EditEntryForm` 占位或下一步再拆——本任务要求壳层文件**仅**负责布局与组合，业务 state 可暂留壳内但若超过 ~120 行须在下个 Task 立即迁出。
    5. `index.ts`：`export { default } from './EditEntryModal'`（或等价）使 `import EditEntryModal from './EditEntryModal'` 仍解析到目录包（EntryCard 旧路径 `./EditEntryModal` 应继续工作）。
    6. 统一 `EditEntryModalProps`：以 `components/types.ts` 或 `types/components.ts` 之一为权威，另一文件 `export type { EditEntryModalProps } from '...'` 避免重复定义分叉。
  </action>
  <verify>
    <automated>cd /Users/moyun/code/Emotion-Diary && yarn lint && yarn test:ci</automated>
  </verify>
  <acceptance_criteria>
    - 不存在 `components/EditEntryModal.tsx` 单文件与 `components/EditEntryModal/` 目录并存冲突。
    - 样式与纯函数已从原单文件迁出；lint/test 通过。
    - 默认导出路径对 `./EditEntryModal` 仍可用。
  </acceptance_criteria>
  <done>目录化完成，壳/样式/纯函数边界清晰（D-14、D-15 部分）。</done>
</task>

<task type="auto">
  <name>Task 2: 分离 EditEntryForm 与 EditEntryFields；新增 @/components/entries 并迁移 EntryCard</name>
  <read_first>
    - components/EditEntryModal/EditEntryModal.tsx（Task 1 结果）
    - components/EntryCard.tsx（import 与 EditEntryModal 使用处）
  </read_first>
  <files>components/EditEntryModal/EditEntryModal.tsx, components/EditEntryModal/EditEntryForm.tsx, components/EditEntryModal/EditEntryFields.tsx, components/entries/index.ts, components/EntryCard.tsx</files>
  <action>
    1. 创建 `EditEntryForm.tsx`：承载 `useAppStore` 的 `updateEntry`、`useHapticFeedback`、所有表单 `useState`、`useEffect`（entry → 表单同步）、`handleSubmit`（校验规则与 `Alert` 文案**逐字**保留）、自定义标签状态机；从壳层 `EditEntryModal.tsx` 移除上述逻辑，壳层只传 `entry`/`visible`/`onClose`/`onSuccess` 给 Form。
    2. 创建 `EditEntryFields.tsx`：接收 Form 下发的 props（value/onChange、标签列表、MOOD_CONFIG 渲染等），**无** store 订阅；复杂度过高时可再拆子文件，但单文件不超过 ~400 行为宜。
    3. 保持 `handleSubmit` 成功后 `onSuccess?.()` 与 `onClose` 延迟（若原有 `setTimeout`）**毫秒数一致**。
    4. 新建 `components/entries/index.ts`：`export { default as EditEntryModal } from '../EditEntryModal'` 或 `export { EditEntryModal }`（与默认导出策略一致）；满足 D-16。
    5. 修改 `components/EntryCard.tsx`：将 `import EditEntryModal from "./EditEntryModal"` 改为 `import { EditEntryModal } from "@/components/entries"`（或 `import EditEntryModal from "@/components/entries"` 若 barrel 提供 default——与实现的 index 一致即可）。
    6. 搜索全仓 `EditEntryModal` import：除 EntryCard 外**不强制**改路径（deferred）；若 `__tests__` 仍指向旧路径，仅当测试失败时更新为可解析路径。
  </action>
  <verify>
    <automated>cd /Users/moyun/code/Emotion-Diary && yarn lint && yarn test:ci</automated>
  </verify>
  <acceptance_criteria>
    - `EntryCard.tsx` 使用 `@/components/entries` 导入（D-16）。
    - Form 含业务逻辑与 store；Fields 无 `useAppStore`。
    - `yarn lint`、`yarn test:ci` 退出码 0。
  </acceptance_criteria>
  <done>ARC-03：视图/逻辑/纯函数分离 + 至少一处新路径迁移完成。</done>
</task>

</tasks>

<verification>
- `yarn lint`、`yarn test:ci` 通过。
- 手测：从列表打开编辑、空内容提交仍提示、合法提交后列表更新、关闭时序与触觉反馈与拆分前一致（08-RESEARCH）。
- 可选：`node scripts/verify-governance-smoke.js` 若项目已配置且 Phase 6 脚本存在则退出码 0。
</verification>

<success_criteria>
- EditEntryModal 目录结构符合 D-14/D-15；对外契约不变。
- D-16：`@/components/entries` 已被真实调用方使用。
</success_criteria>

<output>
After completion, create `.planning/phases/08-structure-refactor/08-03-SUMMARY.md`
</output>
