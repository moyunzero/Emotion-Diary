---
phase: 11-github-repo-hygiene
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - CONTRIBUTING.md
  - README.en.md
  - .env.example
  - .planning/phases/11-github-repo-hygiene/11-02-SUMMARY.md
autonomous: true
requirements: [GH-01, GH-02, GH-03, INT-01, INT-02, INT-03]
must_haves:
  truths:
    - "CONTRIBUTING 中 PR 目标分支为 master，检查命令含 typecheck/lint/test:ci"
    - "README.en 在 Core Features 前有与主 README 对齐的快速上手与 Yarn/master/CI 说明"
    - ".env.example 与代码中 EXPO_PUBLIC_* 引用一致或对未使用变量有明确预留说明"
    - "GH-02 自检结果写入 11-02-SUMMARY.md"
  artifacts:
    - path: .planning/phases/11-github-repo-hygiene/11-02-SUMMARY.md
      provides: "git grep 自检命令与结论（无密钥入仓或已复核）"
  key_links:
    - from: "CONTRIBUTING.md"
      to: ".github/workflows/ci.yml"
      via: "文档描述的命令与 CI 步骤一致"
      pattern: "typecheck"
---

<objective>
完成 **GH-03**（贡献准则与流程对齐）、**GH-01** 英文文档路径、**INT-02**（`.env.example` 与代码一致）、**GH-02**（维护者密钥模式自检与记录）。依赖 Wave 1 已合并的 `typecheck` 与 CI。

Purpose: 覆盖剩余 **GH-01、GH-02、GH-03、INT-01、INT-02、INT-03**  
Output: CONTRIBUTING / README.en / .env.example 更新；`11-02-SUMMARY.md` 记录安全自检；全量三件套通过
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-github-repo-hygiene/11-CONTEXT.md
@.planning/phases/11-github-repo-hygiene/11-RESEARCH.md
@CONTRIBUTING.md
@README.en.md
@README.md
@.env.example
@.github/workflows/ci.yml
@utils/aiService.ts
@lib/supabase.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: CONTRIBUTING 与 CI / 包管理器对齐（GH-03 / INT-01 / INT-03）</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    CONTRIBUTING.md
    .github/workflows/ci.yml
    package.json
    README.md
  </read_first>
  <action>
编辑 `CONTRIBUTING.md`：
1. 将所有「提交 Pull Request 到 **`main` 分支**」类表述改为 **`master` 分支**（子串 `master` 必须出现在 PR 目标说明处）。
2. 在「进行开发」或「代码规范」附近，将单一 `yarn lint` 检查说明扩展为 **显式列表或代码块**，**必须** 逐字包含以下三条命令字符串：`yarn typecheck`、`yarn lint`、`yarn test:ci`。
3. 增加一句：本地安装请使用 **Yarn** 与仓库 `yarn.lock`，CI 使用 `yarn install --frozen-lockfile`（与 INT-03 一致）。
4. 保留 OpenSpec 与 `openspec/` 引用；若文中链接 README 锚点，确保与主 README 中「开发者快速上手」标题不矛盾（可链到 `./README.md` 文件而非易碎锚点）。
  </action>
  <verify>
    <automated>rg -q 'master' CONTRIBUTING.md && rg -q 'yarn typecheck' CONTRIBUTING.md && rg -q 'yarn lint' CONTRIBUTING.md && rg -q 'yarn test:ci' CONTRIBUTING.md && rg -q 'frozen-lockfile|yarn\\.lock' CONTRIBUTING.md</automated>
  </verify>
  <acceptance_criteria>
    - `CONTRIBUTING.md` 含子串 `master` 且 **不含** `提交 Pull Request` 与 `main` 的相邻误导组合（允许英文单词 main 出现在无关上下文；PR 目标必须为 master）
    - `CONTRIBUTING.md` 同时包含 `yarn typecheck`、`yarn lint`、`yarn test:ci` 三个子串
    - `CONTRIBUTING.md` 提及 `frozen-lockfile` 或 `yarn.lock`
  </acceptance_criteria>
  <done>贡献指南与 Wave 1 CI 一致</done>
</task>

<task type="auto">
  <name>Task 2: README.en 快速上手与社区链接（GH-01）</name>
  <files>README.en.md</files>
  <read_first>
    README.en.md
    README.md
    SECURITY.md
    CONTRIBUTING.md
    CODE_OF_CONDUCT.md
  </read_first>
  <action>
编辑 `README.en.md`：
1. 在 **`## 🌟 Core Features` 之前** 插入英文开发者章节（建议标题 `## 👩‍💻 Developer quick start`），**必须**包含：
   - `git clone` / `cd`（目录名与主 README 一致，如 `emotion-diary`）
   - `yarn install` 与 **Yarn + lockfile** 说明
   - 默认分支 **`master`**
   - Copy `.env.example` to `.env`
   - 最小校验：`yarn typecheck`、`yarn lint`、`yarn test:ci`
   - CI 摘要：PR 运行 typecheck/lint/test:ci；push `master` 额外 governance smoke（与 `ci.yml` 一致）
2. 在同一小节或紧随其后的短列表中，加入指向 `./CONTRIBUTING.md`、`./CODE_OF_CONDUCT.md`、`./SECURITY.md` 的 Markdown 链接。
3. 修正文中与仓库不符的 `cd fenyu-emotion-diary` 为与主 README 一致的目录名（若主 README 使用 `emotion-diary` 则英文同步）。
  </action>
  <verify>
    <automated>rg -n 'Developer quick start|For developers' README.en.md | head -1; rg -q 'yarn typecheck' README.en.md && rg -q 'yarn lint' README.en.md && rg -q 'yarn test:ci' README.en.md && rg -q 'master' README.en.md && rg -q 'SECURITY\\.md' README.en.md && rg -q 'CONTRIBUTING\\.md' README.en.md && rg -q 'CODE_OF_CONDUCT\\.md' README.en.md</automated>
  </verify>
  <acceptance_criteria>
    - `Core Features` 二级标题出现在 Developer quick start（或等价标题）**之后**
    - `README.en.md` 含 `yarn typecheck`、`yarn lint`、`yarn test:ci` 子串
    - `README.en.md` 含指向 `SECURITY.md`、`CONTRIBUTING.md`、`CODE_OF_CONDUCT.md` 的相对链接
    - 不再将 `fenyu-emotion-diary` 作为唯一克隆目录名（若仍保留作示例需标注为 optional 并与主 README 一致）
  </acceptance_criteria>
  <done>英文读者路径与中文主 README 对齐</done>
</task>

<task type="auto">
  <name>Task 3: .env.example 与代码引用一致（INT-02）</name>
  <files>.env.example</files>
  <read_first>
    .env.example
    utils/aiService.ts
    lib/supabase.ts
  </read_first>
  <action>
1. 确认 `.env.example` 仍包含且仅强调代码中 **实际读取** 的变量：`EXPO_PUBLIC_GROQ_API_KEY`、`EXPO_PUBLIC_SUPABASE_URL`、`EXPO_PUBLIC_SUPABASE_ANON_KEY`（名称与 `grep process.env.EXPO_PUBLIC_` 结果一致）。
2. 对 `EXPO_PUBLIC_HF_TOKEN`：在 `.env.example` 中该变量 **上方** 增加一行注释，说明 **当前应用代码未引用此变量**、为可选预留（中文注释即可），避免读者认为必须配置。
3. 保留文件底部「不要将 .env 提交」等注意事项；不添加真实密钥。
  </action>
  <verify>
    <automated>rg -q 'EXPO_PUBLIC_GROQ_API_KEY' .env.example && rg -q 'EXPO_PUBLIC_SUPABASE_URL' .env.example && rg -q 'EXPO_PUBLIC_SUPABASE_ANON_KEY' .env.example && rg -q 'HF_TOKEN|Hugging Face' .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` 含 `EXPO_PUBLIC_GROQ_API_KEY`、`EXPO_PUBLIC_SUPABASE_URL`、`EXPO_PUBLIC_SUPABASE_ANON_KEY` 三行模板
    - `.env.example` 中 `EXPO_PUBLIC_HF_TOKEN` 段附近存在说明「未引用」或「预留/可选」含义的中文或英文注释（`rg` 可命中 `未` 或 `optional` 或 `not used` 之一）
  </acceptance_criteria>
  <done>环境模板与运行时读取一致</done>
</task>

<task type="auto">
  <name>Task 4: GH-02 密钥模式自检与 SUMMARY（GH-02）</name>
  <files>.planning/phases/11-github-repo-hygiene/11-02-SUMMARY.md</files>
  <read_first>
    .gitignore
    .planning/phases/11-github-repo-hygiene/11-CONTEXT.md
  </read_first>
  <action>
1. 在仓库根目录执行（或等价 `git grep`）自检，扫描工作区已跟踪内容中的敏感模式，**排除**文档与规划目录示例：

```bash
git grep -niE 'api[_-]?key|client_secret|password\\s*=|BEGIN (RSA |OPENSSH )?PRIVATE KEY' -- ':!*.md' ':!.planning/*' ':!.planning/**' || true
```

2. 新建 `.planning/phases/11-github-repo-hygiene/11-02-SUMMARY.md`，**必须**包含：
   - 标题行 `# Phase 11 Wave 2 — 执行摘要`
   - 小节 `## GH-02 密钥扫描`：粘贴上述命令的完整文本；`## 结果`：写明「无命中」或列出命中文件路径与人工复核结论（确认为示例/假值/可公开）。
   - 小节 `## .gitignore`：一句确认 `.env` 已被忽略（引用 `.gitignore` 中相关行或写「已确认包含 .env」）。
3. 可选：子节 `## GitHub Security` 勾选「Private vulnerability reporting 已在仓库设置中开启」由执行者在合并前人工确认（若无法确认则写「待仓库管理员确认」）。
  </action>
  <verify>
    <automated>test -f .planning/phases/11-github-repo-hygiene/11-02-SUMMARY.md && rg -q 'GH-02' .planning/phases/11-github-repo-hygiene/11-02-SUMMARY.md && rg -q 'git grep' .planning/phases/11-github-repo-hygiene/11-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - 文件 `.planning/phases/11-github-repo-hygiene/11-02-SUMMARY.md` 存在
    - 该文件含 `## GH-02` 或 `GH-02 密钥` 标题/段落
    - 该文件含复制的 `git grep` 命令文本（子串 `git grep`）
    - 该文件含对扫描结果的明确陈述（子串 `无命中` 或 `复核` 或 `假值` 之一）
  </acceptance_criteria>
  <done>GH-02 可审计记录已落盘</done>
</task>

</tasks>

<verification>
本 Wave 结束时执行：`yarn typecheck && yarn lint && yarn test:ci`，均退出码 0。
</verification>

## PLANNING COMPLETE
