---
phase: 11-github-repo-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - SECURITY.md
  - README.md
  - .github/workflows/ci.yml
autonomous: true
requirements: [GH-01, INT-01, INT-03]
must_haves:
  truths:
    - "package.json 含 typecheck 脚本且 yarn typecheck 在干净安装后可运行"
    - "根目录 SECURITY.md 存在且 README 中 ./SECURITY.md 链接有效"
    - "README 在核心功能长文之前提供开发者快速上手与 Yarn/master/CI/最小校验集说明"
    - "CI 在 PR 与 push master 流程中执行 yarn typecheck"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "lint 与 test:ci 之前或之后显式包含 typecheck 步骤（与现有 install 同 job）"
  key_links:
    - from: "README.md"
      to: "CONTRIBUTING.md"
      via: "开发者区块内显式链接"
      pattern: "CONTRIBUTING\\.md"
---

<objective>
落实 Phase 11 **D-10、D-11、D-04、D-05、D-01～D-03** 中与脚本、安全入口、主 README、CI 一致性的部分：新增 `typecheck`、创建 `SECURITY.md`、重组 `README.md`（快速上手前置）、CI 纳入 `yarn typecheck`。为 Wave 2（CONTRIBUTING / README.en / .env.example / GH-02）铺路。

Purpose: **GH-01**（主 README + SECURITY）、**INT-01**（可复现最小校验集）、**INT-03**（Yarn 与锁文件约定写清）  
Output: 上述文件变更；`yarn typecheck && yarn lint && yarn test:ci` 退出码 0
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-github-repo-hygiene/11-CONTEXT.md
@.planning/phases/11-github-repo-hygiene/11-RESEARCH.md
@package.json
@README.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 typecheck 脚本（INT-01 / D-10）</name>
  <files>package.json</files>
  <read_first>
    package.json
    tsconfig.json
    .planning/phases/11-github-repo-hygiene/11-CONTEXT.md
  </read_first>
  <action>
在 `package.json` 的 `"scripts"` 对象中新增 **恰好** 一项（键名与命令不可替换）：
- 键：`typecheck`
- 值：`tsc --noEmit`

推荐插入在 `"lint"` 与 `"build:optimized"` 之间（或按字母顺序紧邻 `test:*` 组上方），不删除或重命名现有脚本。
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.scripts.typecheck||p.scripts.typecheck!=='tsc --noEmit')process.exit(1)" && yarn typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` 文件内容包含子串 `"typecheck": "tsc --noEmit"`（逐字匹配）
    - 在项目根目录执行 `yarn typecheck` 退出码为 0
  </acceptance_criteria>
  <done>typecheck 脚本已添加且本地通过</done>
</task>

<task type="auto">
  <name>Task 2: 新增根目录 SECURITY.md（GH-01 / D-04）</name>
  <files>SECURITY.md</files>
  <read_first>
    README.md
    .planning/phases/11-github-repo-hygiene/11-RESEARCH.md
  </read_first>
  <action>
在仓库根目录 **新建** 文件 `SECURITY.md`（UTF-8 Markdown），正文须为中文或中英并列，篇幅控制在约 1～2 屏，且 **必须** 包含以下可 grep 的固定短语或等价标题（建议用作小节标题）：
1. 一行说明支持范围：包含子串 `当前默认分支` 或 `supported version` 之一。
2. 报告渠道：包含子串 `Security Advisories` 或 `Private vulnerability reporting`（说明若仓库已启用 GitHub 私密漏洞报告则优先使用）。
3. 禁止行为：包含子串 `公开 Issue` 与 `密钥`（提醒勿在公开 Issue 粘贴密钥或真实凭证）。

不要复制真实密钥；不要引用不存在的路径。
  </action>
  <verify>
    <automated>test -f SECURITY.md && rg -q 'Security Advisories|Private vulnerability reporting' SECURITY.md && rg -q '密钥' SECURITY.md</automated>
  </verify>
  <acceptance_criteria>
    - 路径 `SECURITY.md` 存在且为普通文件
    - `SECURITY.md` 含 `Security Advisories` 或 `Private vulnerability reporting` 之一
    - `SECURITY.md` 含子串 `密钥` 与子串 `公开 Issue`
  </acceptance_criteria>
  <done>SECURITY.md 已创建且满足披露与禁止条款</done>
</task>

<task type="auto">
  <name>Task 3: README 前置开发者快速上手并修复安全链接（GH-01 / D-01～D-03 / INT-03）</name>
  <files>README.md</files>
  <read_first>
    README.md
    SECURITY.md
    .planning/phases/11-github-repo-hygiene/11-CONTEXT.md
    openspec/README.md
    .planning/codebase/STACK.md
  </read_first>
  <action>
编辑 `README.md`：
1. 在 **`## 🌟 核心功能` 标题之前** 插入新的二级章节（建议使用标题 `## 👩‍💻 开发者快速上手` 或等价清晰标题），该章节 **必须** 包含：
   - `git clone` 与 `cd` 步骤（`cd` 目录名与仓库常见克隆名一致，可与现有「快速开始」中的 `emotion-diary` 对齐）
   - 显式写 **使用 Yarn**：子串 `yarn install` 与 `--frozen-lockfile` 的说明一句（贡献/CI 与 INT-03）
   - 显式写 **默认分支为 `master`**（子串 `` `master` `` 或「默认分支：master」）
   - 复制环境：子串 `.env.example` 与 `.env`
   - **最小校验集** 三行命令（分开展示或同一代码块）：`yarn typecheck`、`yarn lint`、`yarn test:ci`
   - **CI 行为** 各一行：Pull Request 上运行 `lint`、`typecheck`、`test:ci`；push 到 `master` 另运行 `verify:governance` 与 `verify-governance-smoke`（表述可与 `ci.yml` 一致）
   - 单点贡献入口链接：同一小节或紧邻列表中包含 Markdown 链接 `[CONTRIBUTING.md](./CONTRIBUTING.md)` 与 `[CODE_OF_CONDUCT.md](./CODE_OF_CONDUCT.md)`
   - 深度文档链接：`[openspec/README.md](./openspec/README.md)` 与 `[.planning/codebase/STACK.md](./.planning/codebase/STACK.md)`
2. 保留原有「应用简介」与「核心功能」长文；若原有「## 🚀 快速开始」与新区块重复，将重复的安装步骤改为简短引用新区块（避免维护两处矛盾命令）。
3. 确认「🔒 安全性」小节中指向 `./SECURITY.md` 的链接仍存在且路径正确（任务 2 完成后该链接应可访问）。
  </action>
  <verify>
    <automated>rg -n '开发者快速上手|Developer' README.md | head -1; rg -q 'yarn typecheck' README.md && rg -q 'yarn lint' README.md && rg -q 'yarn test:ci' README.md && rg -q '`master`|默认分支.*master' README.md && rg -q 'CONTRIBUTING\\.md' README.md && rg -q 'CODE_OF_CONDUCT\\.md' README.md && rg -q 'openspec/README\\.md' README.md && rg -q 'STACK\\.md' README.md && rg -q '\\./SECURITY\\.md' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` 中 `## 🌟 核心功能` 出现在「开发者快速上手」对应章节标题 **之后**（用行号或人工阅读确认顺序）
    - `README.md` 同时包含子串 `yarn typecheck`、`yarn lint`、`yarn test:ci`
    - `README.md` 包含 `CONTRIBUTING.md`、`CODE_OF_CONDUCT.md`、`openspec/README.md`、`.planning/codebase/STACK.md` 的 `./` 相对链接形式
    - `README.md` 含默认分支 `master` 的明确表述
    - `README.md` 含 `.env.example` 与 PR/push CI 行为描述（含 `verify:governance` 或 `governance` 关键词）
  </acceptance_criteria>
  <done>主 README 结构满足 D-01～D-03 与 INT 文档承诺</done>
</task>

<task type="auto">
  <name>Task 4: CI 增加 typecheck（INT-01 / 与文档一致）</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    .github/workflows/ci.yml
    package.json
    README.md
  </read_first>
  <action>
在 `.github/workflows/ci.yml` 中，于 **`yarn install --frozen-lockfile` 之后**、`yarn lint` **之前**，为 **pr-gate** 与 **governance** 两个 job 各增加一步：

```yaml
      - run: yarn typecheck
```

保持 YAML 缩进与现有 `uses`/`run` 步骤一致；不修改 Node 版本与 `branches: [master]`。
  </action>
  <verify>
    <automated>rg -n 'yarn typecheck' .github/workflows/ci.yml | wc -l | rg -q '^[2-9]|^1[0-9]'; test $(rg -c 'yarn typecheck' .github/workflows/ci.yml) -ge 2</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` 至少包含 **两处** 独立的 `yarn typecheck` 行（分别位于 `pr-gate` 与 `governance` job）
    - 每处 `yarn typecheck` 出现在同 job 的 `yarn install --frozen-lockfile` 之后、`yarn lint` 之前
  </acceptance_criteria>
  <done>CI 与 README 文档中的最小集一致</done>
</task>

</tasks>

<verification>
本 Wave 结束时在仓库根目录执行：`yarn typecheck && yarn lint && yarn test:ci`，均退出码 0。
</verification>

## PLANNING COMPLETE
