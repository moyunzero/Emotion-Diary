---
phase: 07-shared
plan: 02
type: execute
wave: 3
depends_on:
  - 07-04
files_modified:
  - shared/time-range/periods.ts
  - shared/time-range/presets.ts
  - shared/time-range/index.ts
  - shared/index.ts
  - utils/reviewStatsTimeRange.ts
  - components/Insights/utils.tsx
  - __tests__/unit/shared/time-range/periods.test.ts
  - __tests__/unit/utils/reviewStatsTimeRange.test.ts
autonomous: true
requirements:
  - SHR-01
  - SHR-02
  - SHR-03
must_haves:
  truths:
    - "周/月区间与 preset 文案映射在 shared/time-range 单一来源。"
    - "洞察页与导出页对同一 preset 计算结果一致。"
    - "跨周/跨月/空值边界输入有测试保护。"
  artifacts:
    - path: "shared/time-range/periods.ts"
      provides: "canonical 周月区间与上期区间纯函数"
    - path: "shared/time-range/presets.ts"
      provides: "preset key 与 label 单点映射"
    - path: "utils/reviewStatsTimeRange.ts"
      provides: "旧入口兼容层（re-export/thin adapter）"
  key_links:
    - from: "components/Insights/utils.tsx"
      to: "shared/time-range/periods.ts"
      via: "周边界复用"
      pattern: "from 'shared/time-range'"
    - from: "utils/reviewExportDerived.ts"
      to: "shared/time-range/presets.ts"
      via: "preset 映射统一"
      pattern: "REVIEW_PRESET_LABEL"
---

<objective>
完成 Phase 07 第二包：time-range 收敛与 preset 单点映射，延续兼容层策略并保证统计口径不漂移（D-03）。

Purpose: 把中风险的周期计算与文案映射统一到 shared，消除分散实现造成的“同 key 不同结论”风险。
Output: shared/time-range 模块、旧入口适配、洞察链路消费对齐与边界测试；导出链路迁移下沉到后续小包。
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-shared/07-CONTEXT.md
@.planning/phases/07-shared/07-RESEARCH.md
@utils/reviewStatsTimeRange.ts
@components/Insights/utils.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 shared time-range canonical 实现与边界单测</name>
  <read_first>
    - utils/reviewStatsTimeRange.ts
    - components/Insights/utils.tsx
    - .planning/phases/07-shared/07-RESEARCH.md
  </read_first>
  <files>shared/time-range/periods.ts, shared/time-range/presets.ts, shared/time-range/index.ts, shared/index.ts, __tests__/unit/shared/time-range/periods.test.ts</files>
  <action>新增 `shared/time-range/periods.ts`，集中实现 this_week/last_week/this_month/last_month current+previous 计算；新增 `shared/time-range/presets.ts` 维护 preset->中文 label 单点映射；通过 `shared/time-range/index.ts` 与 `shared/index.ts` 导出。先写失败测试再实现（D-10）：覆盖周一/周日、跨月周、2 月、跨年与空值输入，确保输出区间闭区间语义一致。</action>
  <verify>
    <automated>yarn test:unit __tests__/unit/shared/time-range/periods.test.ts -i</automated>
  </verify>
  <acceptance_criteria>time-range 逻辑与文案映射存在唯一实现入口，边界测试通过且输出可被导出/洞察复用。</acceptance_criteria>
  <done>形成可替换现有分散实现的 canonical 模块，满足 D-01 第二阶段。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 旧入口适配并仅替换洞察链路调用点</name>
  <read_first>
    - utils/reviewStatsTimeRange.ts
    - components/Insights/utils.tsx
  </read_first>
  <files>utils/reviewStatsTimeRange.ts, components/Insights/utils.tsx, __tests__/unit/utils/reviewStatsTimeRange.test.ts</files>
  <action>将 `utils/reviewStatsTimeRange.ts` 转为 shared/time-range 薄适配器并标注 deprecate（D-04/D-06）；把 `components/Insights/utils.tsx` 的 `getWeekDates` 重构为复用 shared 周边界；禁止新增旧路径 import（D-05）。本包仅覆盖洞察链路，导出链路在后续独立小包迁移（增量可回滚，D-02）。</action>
  <verify>
    <automated>yarn test:unit __tests__/unit/utils/reviewStatsTimeRange.test.ts -i</automated>
  </verify>
  <acceptance_criteria>洞察链路使用 shared/time-range；旧入口仅做适配；无新增旧路径 import。</acceptance_criteria>
  <done>time-range 洞察链路迁移完成，导出链路可独立推进与回滚。</done>
</task>

<task type="auto">
  <name>Task 3: 洞察链路统计口径一致性检查</name>
  <read_first>
    - .planning/phases/07-shared/07-CONTEXT.md
    - __tests__/unit/utils/reviewStatsTimeRange.test.ts
  </read_first>
  <files>__tests__/unit/utils/reviewStatsTimeRange.test.ts</files>
  <action>按 D-03 执行洞察链路统计口径一致检查：在测试中固定 same input 比较迁移前后 current/previous 结果，确保核心数值与业务结论一致（D-07）；只允许非语义展示微调（D-08）。导出链路最小回归放入后续计划，避免大包耦合（D-02）。</action>
  <verify>
    <automated>yarn test:unit __tests__/unit/utils/reviewStatsTimeRange.test.ts -i</automated>
  </verify>
  <acceptance_criteria>洞察链路回归通过；周期计算结论一致；无语义偏移。</acceptance_criteria>
  <done>time-range 洞察侧形成可独立发布/回滚小包。</done>
</task>

</tasks>

<verification>
- `yarn lint`
- `yarn test:unit __tests__/unit/shared/time-range/periods.test.ts __tests__/unit/utils/reviewStatsTimeRange.test.ts -i`
</verification>

<success_criteria>
1. time-range 与 preset 映射单一来源化。  
2. 洞察链路在同一输入下业务结论一致。  
3. 跨周跨月与空值边界有自动化保护，且导出链路可后续独立迁移。
</success_criteria>

<output>
After completion, create `.planning/phases/07-shared/07-02-SUMMARY.md`
</output>
