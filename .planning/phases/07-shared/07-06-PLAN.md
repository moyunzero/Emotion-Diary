---
phase: 07-shared
plan: 06
type: execute
wave: 6
depends_on:
  - 07-03
files_modified:
  - components/ReviewExport/ReviewExportScreen.tsx
  - components/Insights/EmotionTrends.tsx
  - __tests__/unit/components/ReviewExportScreen.responsive.test.tsx
autonomous: true
requirements:
  - SHR-02
  - SHR-03
must_haves:
  truths:
    - "responsive 关键页迁移后，关键断点行为无回归。"
    - "关键页迁移包可独立回滚，不影响 responsive 核心与兼容层。"
  artifacts:
    - path: "components/ReviewExport/ReviewExportScreen.tsx"
      provides: "导出页改用 shared/hook 响应式值"
    - path: "components/Insights/EmotionTrends.tsx"
      provides: "洞察页改用 shared/hook 响应式值"
    - path: "__tests__/unit/components/ReviewExportScreen.responsive.test.tsx"
      provides: "关键断点最小回归"
  key_links:
    - from: "components/ReviewExport/ReviewExportScreen.tsx"
      to: "hooks/useResponsiveStyles.ts"
      via: "关键页统一消费 hook"
      pattern: "useResponsiveStyles"
---

<objective>
在 07-03 核心稳定后，独立完成 responsive 关键页迁移与最小回归。

Purpose: 响应 scope_sanity，先稳核心再动关键页，保证每包可回滚。
Output: 导出页/洞察页响应式迁移 + 关键断点回归测试。
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-shared/07-03-PLAN.md
@components/ReviewExport/ReviewExportScreen.tsx
@components/Insights/EmotionTrends.tsx
@__tests__/unit/components/ReviewExportScreen.responsive.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: responsive 关键页迁移（导出页 + 洞察页）</name>
  <read_first>
    - .planning/phases/07-shared/07-03-PLAN.md
    - components/ReviewExport/ReviewExportScreen.tsx
    - components/Insights/EmotionTrends.tsx
  </read_first>
  <files>components/ReviewExport/ReviewExportScreen.tsx, components/Insights/EmotionTrends.tsx</files>
  <action>将导出页与洞察页的响应式参数读取统一迁移到 `hooks/useResponsiveStyles`/`shared/responsive`（承接 D-01）；不改 shared 核心逻辑与兼容层，仅调整关键页消费侧（D-02）。禁止新增旧入口调用（D-05）。</action>
  <verify>
    <automated>yarn test:unit __tests__/unit/hooks/useResponsiveStyles.test.ts -i</automated>
  </verify>
  <acceptance_criteria>关键页迁移完成；响应式值来源统一；页面层 diff 小且可独立回滚。</acceptance_criteria>
  <done>responsive 关键页迁移完成，与 07-03 解耦。</done>
</task>

<task type="auto">
  <name>Task 2: 关键断点最小回归与一致性检查</name>
  <read_first>
    - __tests__/unit/components/ReviewExportScreen.responsive.test.tsx
    - .planning/phases/07-shared/07-CONTEXT.md
  </read_first>
  <files>__tests__/unit/components/ReviewExportScreen.responsive.test.tsx</files>
  <action>补齐关键断点（小屏/平板）布局参数断言，验证迁移后间距与字号级别行为稳定；按 D-03 执行业务一致检查，确保响应式迁移不影响业务结论（D-07/D-08），并满足关键页最小回归要求（D-09）。</action>
  <verify>
    <automated>yarn test:unit __tests__/unit/components/ReviewExportScreen.responsive.test.tsx __tests__/unit/hooks/useResponsiveStyles.test.ts -i</automated>
  </verify>
  <acceptance_criteria>关键断点回归通过；业务结论未受影响；该包可独立回滚。</acceptance_criteria>
  <done>responsive 阶段“核心+兼容层→关键页迁移+回归”微拆闭环完成。</done>
</task>

</tasks>

<verification>
- `yarn lint`
- `yarn test:unit __tests__/unit/components/ReviewExportScreen.responsive.test.tsx __tests__/unit/hooks/useResponsiveStyles.test.ts -i`
</verification>

<success_criteria>
1. responsive 关键页迁移完成，行为在关键断点稳定。  
2. 页面迁移包与 07-03 基线解耦，可独立回滚。  
3. SHR-02/SHR-03 在 responsive 关键页路径获得验证。
</success_criteria>

<output>
After completion, create `.planning/phases/07-shared/07-06-SUMMARY.md`
</output>
