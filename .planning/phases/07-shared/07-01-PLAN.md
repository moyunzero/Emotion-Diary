---
phase: 07-shared
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/formatting/date.ts
  - shared/formatting/index.ts
  - shared/index.ts
  - utils/dateUtils.ts
  - services/companionDaysService.ts
  - __tests__/unit/shared/formatting/date.test.ts
  - __tests__/unit/utils/dateUtils.test.ts
  - __tests__/unit/services/companionDaysService.test.ts
autonomous: true
requirements:
  - SHR-01
  - SHR-02
  - SHR-03
must_haves:
  truths:
    - "格式化逻辑由 shared 单一入口提供，旧入口在 Phase 7 内仍可调用。"
    - "导出页与洞察页的日期/文本语义与迁移前一致。"
    - "格式化边界输入（空值、跨月、跨年）可被自动化测试覆盖。"
  artifacts:
    - path: "shared/formatting/date.ts"
      provides: "统一日期/短日期/相对时间格式化纯函数"
    - path: "utils/dateUtils.ts"
      provides: "旧入口兼容层（thin adapter）"
    - path: "services/companionDaysService.ts"
      provides: "旧 formatStartDate 转调 shared"
  key_links:
    - from: "components/ReviewExport/ReviewExportScreen.tsx"
      to: "shared/formatting/index.ts"
      via: "统一格式化导入"
      pattern: "from 'shared/formatting'"
    - from: "utils/dateUtils.ts"
      to: "shared/formatting/date.ts"
      via: "兼容层转调"
      pattern: "export.*=.*shared"
---

<objective>
完成 Phase 07 的 formatting 收敛首包（D-01），建立 shared 单一来源并保留兼容层到 Phase 7 结束（D-04/D-06）。

Purpose: 先做低风险统一，降低日期/文本格式语义漂移风险，同时保持业务一致口径（D-07/D-08）。
Output: shared formatting 模块、旧入口 thin adapter、边界单测；关键页替换与最小回归下沉到后续小包。
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-shared/07-CONTEXT.md
@.planning/phases/07-shared/07-RESEARCH.md
@utils/dateUtils.ts
@services/companionDaysService.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 shared formatting 单一入口并补齐边界测试</name>
  <read_first>
    - .planning/phases/07-shared/07-CONTEXT.md
    - .planning/phases/07-shared/07-RESEARCH.md
    - utils/dateUtils.ts
    - services/companionDaysService.ts
  </read_first>
  <files>shared/formatting/date.ts, shared/formatting/index.ts, shared/index.ts, __tests__/unit/shared/formatting/date.test.ts</files>
  <action>新增 `shared/formatting/date.ts`，实现 `formatDateChinese(timestamp|Date|null)`、`formatMonthDay(timestamp|Date|null)`、`formatRelativeDayLabel(inputDate, now)` 三个纯函数；空值返回稳定兜底字符串，不抛异常。新增 `shared/formatting/index.ts` 与 `shared/index.ts` 导出入口（按领域分组、可 tree-shaking，符合 D-01 与 discretion）。先写失败测试再实现（D-10）：覆盖空值、2 月、闰年、跨年场景，并断言输出语义稳定。</action>
  <verify>
    <automated>yarn test:unit __tests__/unit/shared/formatting/date.test.ts -i</automated>
  </verify>
  <acceptance_criteria>shared formatting 入口可被导入；边界测试全部通过；没有在 shared 外新增重复格式化实现。</acceptance_criteria>
  <done>formatting 单一来源建立完成，可支撑旧入口适配与页面替换。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 旧入口改为 thin adapter（不触及关键页）</name>
  <read_first>
    - utils/dateUtils.ts
    - services/companionDaysService.ts
  </read_first>
  <files>utils/dateUtils.ts, services/companionDaysService.ts, __tests__/unit/utils/dateUtils.test.ts, __tests__/unit/services/companionDaysService.test.ts</files>
  <action>将 `utils/dateUtils.ts` 与 `services/companionDaysService.ts` 旧导出保留函数签名但内部统一转调 `shared/formatting`（D-04/D-06）；给旧导出加 `@deprecated Phase 7 temporary compatibility` 注释。此包不改关键页调用，仅完成兼容层和一致性测试，确保可回滚粒度更小（D-02）。</action>
  <verify>
    <automated>yarn test:unit __tests__/unit/utils/dateUtils.test.ts __tests__/unit/services/companionDaysService.test.ts -i</automated>
  </verify>
  <acceptance_criteria>旧 API 未破坏；旧入口仅保留适配代码无业务实现；与 shared 输出一致。</acceptance_criteria>
  <done>兼容层生效，作为关键页迁移前的稳定基线包。</done>
</task>

</tasks>

<verification>
- `yarn lint`
- `yarn test:unit __tests__/unit/shared/formatting/date.test.ts __tests__/unit/utils/dateUtils.test.ts __tests__/unit/services/companionDaysService.test.ts -i`
</verification>

<success_criteria>
1. formatting 逻辑具备 shared 单一入口，旧入口在 Phase 7 内继续可用。  
2. 旧入口在 Phase 7 内保持兼容，且仅保留适配逻辑。  
3. 关键边界输入均有自动化覆盖并通过。
</success_criteria>

<output>
After completion, create `.planning/phases/07-shared/07-01-SUMMARY.md`
</output>
