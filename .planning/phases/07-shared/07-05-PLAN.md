---
phase: 07-shared
plan: 05
type: execute
wave: 4
depends_on:
  - 07-02
files_modified:
  - utils/reviewExportDerived.ts
  - components/ReviewExport/ReviewExportScreen.tsx
  - __tests__/unit/utils/reviewExportDerived.test.ts
autonomous: true
requirements:
  - SHR-02
  - SHR-03
must_haves:
  truths:
    - "time-range 导出链路与洞察链路共享同一 preset/period 语义。"
    - "导出链路迁移可独立回滚，不影响 07-02 洞察链路。"
  artifacts:
    - path: "utils/reviewExportDerived.ts"
      provides: "导出链路改用 shared/time-range"
    - path: "components/ReviewExport/ReviewExportScreen.tsx"
      provides: "导出页 preset 文案来源统一"
  key_links:
    - from: "utils/reviewExportDerived.ts"
      to: "shared/time-range/presets.ts"
      via: "preset label 映射统一"
      pattern: "from 'shared/time-range'"
---

<objective>
完成 time-range 的导出链路独立迁移，和 07-02（洞察链路）形成可单独发布/回滚的两段小包。

Purpose: 响应 scope_sanity，把“洞察链路迁移”与“导出链路迁移”拆开执行。
Output: 导出链路 time-range/preset 统一 + 最小回归。
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-shared/07-02-PLAN.md
@utils/reviewExportDerived.ts
@components/ReviewExport/ReviewExportScreen.tsx
@__tests__/unit/utils/reviewExportDerived.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 迁移导出链路到 shared/time-range</name>
  <read_first>
    - .planning/phases/07-shared/07-02-PLAN.md
    - utils/reviewExportDerived.ts
    - components/ReviewExport/ReviewExportScreen.tsx
  </read_first>
  <files>utils/reviewExportDerived.ts, components/ReviewExport/ReviewExportScreen.tsx</files>
  <action>把导出链路中周期计算与 preset 文案统一接入 `shared/time-range`（承接 D-01）；移除本地重复映射并禁止新增旧路径 import（D-05）。保持改动聚焦导出链路，不回改洞察链路（增量可回滚，D-02）。</action>
  <verify>
    <automated>yarn test:unit __tests__/unit/utils/reviewExportDerived.test.ts -i</automated>
  </verify>
  <acceptance_criteria>导出链路统一走 shared/time-range；重复 preset 映射被消除；洞察链路不受影响。</acceptance_criteria>
  <done>time-range 导出迁移完成，与 07-02 保持解耦。</done>
</task>

<task type="auto">
  <name>Task 2: 导出链路口径一致性回归</name>
  <read_first>
    - __tests__/unit/utils/reviewExportDerived.test.ts
    - .planning/phases/07-shared/07-CONTEXT.md
  </read_first>
  <files>__tests__/unit/utils/reviewExportDerived.test.ts</files>
  <action>按 D-03 固定输入执行导出链路 current/previous 断言，确保业务结论不漂移（D-07）；仅允许展示文案非语义微调（D-08），并维持关键路径最小回归（D-09）。</action>
  <verify>
    <automated>yarn test:unit __tests__/unit/utils/reviewExportDerived.test.ts -i</automated>
  </verify>
  <acceptance_criteria>导出链路核心断言通过；与迁移前结论一致；可独立回滚。</acceptance_criteria>
  <done>time-range 阶段“洞察/导出分拆迁移”闭环完成。</done>
</task>

</tasks>

<verification>
- `yarn lint`
- `yarn test:unit __tests__/unit/utils/reviewExportDerived.test.ts -i`
</verification>

<success_criteria>
1. time-range 导出链路迁移完成且与洞察链路同语义。  
2. 迁移包与 07-02 解耦，支持独立回滚。  
3. SHR-02/SHR-03 在 time-range 导出路径完成覆盖。
</success_criteria>

<output>
After completion, create `.planning/phases/07-shared/07-05-SUMMARY.md`
</output>
