---
phase: 07-shared
plan: 03
type: execute
wave: 5
depends_on:
  - 07-05
files_modified:
  - shared/responsive/metrics.ts
  - shared/responsive/tokens.ts
  - shared/responsive/index.ts
  - shared/index.ts
  - utils/responsiveUtils.ts
  - hooks/useResponsiveStyles.ts
  - __tests__/unit/shared/responsive/metrics.test.ts
  - __tests__/unit/hooks/useResponsiveStyles.test.ts
autonomous: true
requirements:
  - SHR-01
  - SHR-02
  - SHR-03
must_haves:
  truths:
    - "responsive 逻辑由 shared/responsive 单一入口提供。"
    - "导出页与洞察页在关键断点下布局和字号行为不回归。"
    - "旧 responsive 入口在 Phase 7 内可用，并可在 Phase 8 删除。"
  artifacts:
    - path: "shared/responsive/metrics.ts"
      provides: "输入 width/height 的设备类型与尺寸计算纯函数"
    - path: "utils/responsiveUtils.ts"
      provides: "旧 API 兼容层"
    - path: "hooks/useResponsiveStyles.ts"
      provides: "hook adapter 统一消费 shared/responsive"
  key_links:
    - from: "hooks/useResponsiveStyles.ts"
      to: "shared/responsive/metrics.ts"
      via: "实时尺寸转调"
      pattern: "useWindowDimensions"
    - from: "components/ReviewExport/ReviewExportScreen.tsx"
      to: "hooks/useResponsiveStyles.ts"
      via: "关键页布局消费"
      pattern: "useResponsiveStyles"
---

<objective>
完成 Phase 07 第三包：responsive 收敛到 shared 单一入口并完成关键页最小回归，保持兼容层到本阶段结束（D-04）。

Purpose: 消除响应式阈值散落与静态尺寸快照风险，保证 UI 在关键断点下稳定可预期。
Output: shared/responsive 模块、旧入口适配、hook 对齐与边界测试；关键页迁移与回归拆到后续独立小包。
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-shared/07-CONTEXT.md
@.planning/phases/07-shared/07-RESEARCH.md
@utils/responsiveUtils.ts
@hooks/useResponsiveStyles.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 shared responsive 核心与断点边界测试</name>
  <read_first>
    - utils/responsiveUtils.ts
    - hooks/useResponsiveStyles.ts
    - .planning/phases/07-shared/07-RESEARCH.md
  </read_first>
  <files>shared/responsive/metrics.ts, shared/responsive/tokens.ts, shared/responsive/index.ts, shared/index.ts, __tests__/unit/shared/responsive/metrics.test.ts</files>
  <action>新增 `shared/responsive/metrics.ts`，实现基于 `width/height` 输入的设备类型、间距与字体级别计算；新增 `shared/responsive/tokens.ts` 统一断点 token；通过 `shared/responsive/index.ts` 与 `shared/index.ts` 对外导出。先写失败测试再实现（D-10），覆盖小屏/平板阈值、横竖屏尺寸切换输入，禁止模块加载期固定 `Dimensions.get('window')` 作为核心逻辑。</action>
  <verify>
    <automated>yarn test:unit __tests__/unit/shared/responsive/metrics.test.ts -i</automated>
  </verify>
  <acceptance_criteria>responsive 核心改为输入驱动纯函数；关键断点测试通过；可被 hook 与组件复用。</acceptance_criteria>
  <done>建立 responsive 单一来源，满足 D-01 第三阶段。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 兼容层改造与 hook 收敛（不触及关键页）</name>
  <read_first>
    - utils/responsiveUtils.ts
    - hooks/useResponsiveStyles.ts
  </read_first>
  <files>utils/responsiveUtils.ts, hooks/useResponsiveStyles.ts, __tests__/unit/hooks/useResponsiveStyles.test.ts</files>
  <action>把 `utils/responsiveUtils.ts` 旧 API 改为转调 shared/responsive 并标注临时兼容（D-04/D-06）；`hooks/useResponsiveStyles.ts` 统一接入 shared/responsive，使用 `useWindowDimensions` 实时输入；禁止新增旧入口调用（D-05）。本包不改关键页组件，仅建立稳定兼容基础与 hook 一致性（D-02）。</action>
  <verify>
    <automated>yarn test:unit __tests__/unit/hooks/useResponsiveStyles.test.ts -i</automated>
  </verify>
  <acceptance_criteria>旧入口仅适配；hook 统一调用 shared/responsive；旋转/尺寸变化下计算来源统一。</acceptance_criteria>
  <done>responsive 核心+兼容层基线建立，可供关键页迁移小包复用。</done>
</task>

</tasks>

<verification>
- `yarn lint`
- `yarn test:unit __tests__/unit/shared/responsive/metrics.test.ts __tests__/unit/hooks/useResponsiveStyles.test.ts -i`
</verification>

<success_criteria>
1. responsive 逻辑在 shared 形成唯一入口，旧入口仅作为 Phase 7 临时兼容层。  
2. shared/responsive 核心与兼容层完成收敛，关键页迁移可独立推进。  
3. 边界输入自动化可在后续关键页迁移前提供稳定护栏。
</success_criteria>

<output>
After completion, create `.planning/phases/07-shared/07-03-SUMMARY.md`
</output>
