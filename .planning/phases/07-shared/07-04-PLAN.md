---
phase: 07-shared
plan: 04
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - components/ReviewExport/ReviewExportScreen.tsx
  - components/Insights/TriggerInsight.tsx
  - __tests__/unit/utils/reviewExportDerived.test.ts
autonomous: true
requirements:
  - SHR-02
  - SHR-03
must_haves:
  truths:
    - "formatting 关键页迁移后，导出页与洞察页日期语义与迁移前一致。"
    - "本批可单独回滚，不影响 shared formatting 与兼容层基线。"
  artifacts:
    - path: "components/ReviewExport/ReviewExportScreen.tsx"
      provides: "导出页改用 shared/formatting"
    - path: "components/Insights/TriggerInsight.tsx"
      provides: "洞察页改用 shared/formatting"
    - path: "__tests__/unit/utils/reviewExportDerived.test.ts"
      provides: "导出链路最小回归保护"
  key_links:
    - from: "components/ReviewExport/ReviewExportScreen.tsx"
      to: "shared/formatting/index.ts"
      via: "统一格式化导入"
      pattern: "from 'shared/formatting'"
---

<objective>
在不改 shared 核心实现的前提下，完成 formatting 关键页替换与最小回归（小包、可回滚）。

Purpose: 将“兼容层准备”与“页面替换”拆分，降低变更面并提升回滚速度。
Output: 导出页/洞察页迁移 + 导出链路回归测试。
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-shared/07-01-PLAN.md
@components/ReviewExport/ReviewExportScreen.tsx
@components/Insights/TriggerInsight.tsx
@__tests__/unit/utils/reviewExportDerived.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: formatting 关键页迁移（导出页 + 洞察页）</name>
  <read_first>
    - .planning/phases/07-shared/07-01-PLAN.md
    - components/ReviewExport/ReviewExportScreen.tsx
    - components/Insights/TriggerInsight.tsx
  </read_first>
  <files>components/ReviewExport/ReviewExportScreen.tsx, components/Insights/TriggerInsight.tsx</files>
  <action>将导出页与洞察页的日期/文本格式化调用统一替换为 `shared/formatting`（延续 D-01，符合 D-05）；不修改 shared 核心与兼容层实现，只处理关键页 import 与调用映射，保持 diff 小、可快速回滚（D-02）。</action>
  <verify>
    <automated>yarn test:unit __tests__/unit/utils/reviewExportDerived.test.ts -i</automated>
  </verify>
  <acceptance_criteria>关键页不再新增旧入口 import；UI 文案语义保持一致；变更集中在页面层。</acceptance_criteria>
  <done>formatting 页面替换完成，且可与 07-01 基线解耦回滚。</done>
</task>

<task type="auto">
  <name>Task 2: 执行导出链路最小回归与口径一致检查</name>
  <read_first>
    - __tests__/unit/utils/reviewExportDerived.test.ts
    - .planning/phases/07-shared/07-CONTEXT.md
  </read_first>
  <files>__tests__/unit/utils/reviewExportDerived.test.ts</files>
  <action>按 D-03 增补/更新导出链路断言，验证迁移后业务结论保持一致（D-07），仅允许非语义展示微调（D-08）；回归范围限制在关键路径（D-09），维持小包策略。</action>
  <verify>
    <automated>yarn test:unit __tests__/unit/utils/reviewExportDerived.test.ts -i</automated>
  </verify>
  <acceptance_criteria>导出链路回归通过；核心统计结论不变；该包独立回滚不影响 07-01。</acceptance_criteria>
  <done>formatting 阶段完成“兼容层→关键页”的微拆闭环。</done>
</task>

</tasks>

<verification>
- `yarn lint`
- `yarn test:unit __tests__/unit/utils/reviewExportDerived.test.ts -i`
</verification>

<success_criteria>
1. formatting 关键页替换完成且保持语义一致。  
2. 回归验证集中在关键路径，包体可独立回滚。  
3. SHR-02/SHR-03 在 formatting 阶段被实质覆盖。
</success_criteria>

<output>
After completion, create `.planning/phases/07-shared/07-04-SUMMARY.md`
</output>
