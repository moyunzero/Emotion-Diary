---
phase: 09-dir-boundary-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-governance.js
  - scripts/governance/depcruise.cjs
  - scripts/governance/allowlist.knip.json
  - eslint.config.js
  - .planning/phases/06-governance-baseline-gates/06-SMOKE-CHECKLIST.md
  - .planning/codebase/STRUCTURE.md
autonomous: true
requirements: [CLN-01]
must_haves:
  truths:
    - "features/ 与 shared/ 已纳入治理 scope"
    - "三项规则（跨层越界、循环依赖、未使用导出）已升 error"
    - "depcruise 使用 scripts/governance/depcruise.cjs 配置"
    - "verify:governance 全绿可复现"
  artifacts:
    - path: scripts/verify-governance.js
      provides: "GOVERNANCE_SCOPE 含 features, shared；depcruise 传 --config"
    - path: scripts/governance/depcruise.cjs
      provides: "features 规则；三项规则 severity=error"
    - path: eslint.config.js
      provides: "features/shared boundaries；features 不得 import app/features"
    - path: .planning/codebase/STRUCTURE.md
      provides: "shared/utils 边界说明"
  key_links:
    - from: scripts/verify-governance.js
      to: scripts/governance/depcruise.cjs
      via: depcruise --config
      pattern: "--config.*depcruise"
---

<objective>
扩展治理 scope 至 features/ 与 shared/，定义 app→features→下层 分层规则，并将三项门禁（跨层越界、循环依赖、未使用导出）从 warn 升级到 error。确保 verify:governance 可本地/CI 稳定复现。

Purpose: CLN-01 — 建立目录边界规则并执行依赖检查，避免新增跨层耦合与循环依赖
Output: 更新后的 verify-governance.js、depcruise.cjs、allowlist.knip.json、eslint.config.js；06-SMOKE-CHECKLIST 扩展删除/迁移路径检查项；STRUCTURE.md 补充 shared/utils 边界
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-dir-boundary-cleanup/09-CONTEXT.md
@.planning/phases/09-dir-boundary-cleanup/09-RESEARCH.md
@.planning/phases/06-governance-baseline-gates/06-GATE-RULES.md
@scripts/governance/depcruise.cjs
@scripts/verify-governance.js
@eslint.config.js
@scripts/governance/allowlist.knip.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend scope and depcruise config</name>
  <files>scripts/verify-governance.js, scripts/governance/depcruise.cjs, scripts/governance/allowlist.knip.json</files>
  <read_first>
    scripts/verify-governance.js
    scripts/governance/depcruise.cjs
    scripts/governance/allowlist.knip.json
  </read_first>
  <action>
1. 在 scripts/verify-governance.js 中：
   - 将 GOVERNANCE_SCOPE 扩展为包含 "features", "shared"（顺序保持：app, components, store, utils, hooks, services, lib, features, shared）
   - 修改 dependency-cruiser 阶段的 args：在 depcruise 命令中加入 `--config scripts/governance/depcruise.cjs`，然后传入 ...GOVERNANCE_SCOPE 作为待分析路径
   - 具体：args: ["depcruise", "--config", "scripts/governance/depcruise.cjs", ...GOVERNANCE_SCOPE]

2. 在 scripts/governance/depcruise.cjs 中：
   - 将 GOVERNANCE_SCOPE 数组扩展：在现有项后追加 "^features/", "^shared/"
   - 为 no-cross-layer-boundary、no-new-circular、no-new-unused-export 的 severity 从 "warn" 改为 "error"（per D-03）
   - 新增 forbidden 规则：
     - name: "no-features-to-app", from: { path: "^features/" }, to: { path: "^app/" }, severity: "error", comment: "features 不可 import app"
     - name: "no-features-to-features", from: { path: "^features/" }, to: { path: "^features/" }, severity: "error", comment: "features 之间不可互相 import"
   - metadata.scope 数组追加 "features", "shared"

3. 在 scripts/governance/allowlist.knip.json 中：
   - 将 scope 数组扩展：追加 "features", "shared"
  </action>
  <verify>
    <automated>npm run verify:governance 2>&1 | head -50</automated>
  </verify>
  <acceptance_criteria>
    - scripts/verify-governance.js 中 GOVERNANCE_SCOPE 包含字符串 "features" 和 "shared"
    - scripts/verify-governance.js 中 dependency-cruiser 的 args 包含 "--config" 与 "scripts/governance/depcruise.cjs"
    - scripts/governance/depcruise.cjs 中 no-cross-layer-boundary、no-new-circular、no-new-unused-export 的 severity 为 "error"
    - scripts/governance/depcruise.cjs 中存在 "no-features-to-app" 与 "no-features-to-features" 规则
    - scripts/governance/allowlist.knip.json 的 scope 数组包含 "features" 和 "shared"
  </acceptance_criteria>
  <done>Scope 扩展完成；depcruise 使用正确 config；三项规则已升 error；features 分层规则已添加</done>
</task>

<task type="auto">
  <name>Task 2: Extend eslint boundaries for features and shared</name>
  <files>eslint.config.js</files>
  <read_first>
    eslint.config.js
    .planning/phases/09-dir-boundary-cleanup/09-RESEARCH.md
  </read_first>
  <action>
1. 在 eslint.config.js 的 settings['boundaries/elements'] 中追加：
   { type: 'features', pattern: 'features/**' },
   { type: 'shared', pattern: 'shared/**' },

2. 在 files 数组中加入 features 与 shared 的 glob：
   'features/**/*.{ts,tsx,js,jsx}', 'shared/**/*.{ts,tsx,js,jsx}'

3. 在 rules['boundaries/element-types'] 的 rules 数组中追加（per D-02, D-04）：
   { from: ['features'], disallow: ['app'], message: 'GOV boundary: features 不可依赖 app。' },
   { from: ['features'], disallow: ['features'], message: 'GOV boundary: features 之间不可互相引用。' },
   { from: ['shared'], disallow: ['app', 'features', 'components', 'store'], message: 'GOV boundary: shared 为纯函数层，不可依赖业务层。' }

4. Phase 9 要求三项门禁全 error：将 governanceGateLevel 默认改为 'error'（或保持通过 GOV_BOUNDARIES_LEVEL=error 启用）。为与 D-03 一致，本阶段将 boundaries 默认设为 error：把 `process.env.GOV_BOUNDARIES_LEVEL === 'error' ? 'error' : 'warn'` 改为 `'error'`，使 Phase 9 后 boundaries 默认阻断违规。
  </action>
  <verify>
    <automated>npx eslint app features shared components store utils hooks services lib --ext .ts,.tsx,.js,.jsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - eslint.config.js 的 settings['boundaries/elements'] 包含 type: 'features' 与 type: 'shared'
    - rules['boundaries/element-types'] 的 rules 包含 from: ['features'], disallow: ['app'] 与 from: ['features'], disallow: ['features']
    - 存在针对 shared 的 disallow 规则（不可依赖 app/features/components/store）
  </acceptance_criteria>
  <done>eslint boundaries 已纳入 features 与 shared；features 不得 import app/features；shared 不得依赖业务层</done>
</task>

<task type="auto">
  <name>Task 3: Extend smoke checklist and document shared/utils boundary</name>
  <files>.planning/phases/06-governance-baseline-gates/06-SMOKE-CHECKLIST.md, .planning/codebase/STRUCTURE.md</files>
  <read_first>
    .planning/phases/06-governance-baseline-gates/06-SMOKE-CHECKLIST.md
    .planning/codebase/STRUCTURE.md
    .planning/phases/09-dir-boundary-cleanup/09-CONTEXT.md
  </read_first>
  <action>
1. 在 06-SMOKE-CHECKLIST.md 中新增「删除/迁移路径」检查项（per D-14）：
   - 在「路径 1：记录」后或文末添加新节「路径 4：删除/迁移路径（Phase 9）」
   - 内容：验证 responsiveUtils、dateUtils、reviewStatsTimeRange 迁移后，导出、洞察、记录等关键页无引用断裂
   - 自动化：`node scripts/verify-governance-smoke.js` 已覆盖 export 路径；补充说明「迁移完成后需确认无 utils/responsiveUtils、utils/dateUtils、utils/reviewStatsTimeRange 的 import 残留」
   - 手测：打开洞察页、导出页、记录页，确认无白屏或控制台报错

2. 在 .planning/codebase/STRUCTURE.md 中补充 shared/utils 边界（per D-12）：
   - 在「命名约定」后或合适位置新增小节「Shared 与 Utils 边界」
   - 内容：新纯函数进 shared/（formatting、time-range、responsive 等）；utils/ 仅遗留/桥接，不新增业务逻辑； deprecated 适配层在 Phase 9 清零
  </action>
  <verify>
    <automated>grep -E "删除/迁移路径|Shared 与 Utils 边界|responsiveUtils|dateUtils|reviewStatsTimeRange" ".planning/phases/06-governance-baseline-gates/06-SMOKE-CHECKLIST.md" ".planning/codebase/STRUCTURE.md"</automated>
  </verify>
  <acceptance_criteria>
    - 06-SMOKE-CHECKLIST.md 包含「删除/迁移路径」或「路径 4」相关段落
    - 06-SMOKE-CHECKLIST.md 提到 responsiveUtils、dateUtils、reviewStatsTimeRange 迁移验证
    - STRUCTURE.md 包含「Shared 与 Utils 边界」或等价小节
    - STRUCTURE.md 写明「新纯函数进 shared」「utils 仅遗留/桥接」
  </acceptance_criteria>
  <done>Smoke 清单已扩展；shared/utils 边界已文档化</done>
</task>

<task type="auto">
  <name>Task 4: Fix violations and ensure verify:governance passes</name>
  <files>(variable - any file with violations)</files>
  <read_first>
    scripts/verify-governance.js
    scripts/governance/depcruise.cjs
  </read_first>
  <action>
1. 运行 `npm run verify:governance`，若有失败：
   - depcruise 违规：按报告修复 import 或补充 allowlist 条目（仅当确属历史债且有理由）
   - knip 违规：按报告修复或更新 allowlist.knip.json
   - eslint boundaries 违规：修复 import 路径，使 features 不引用 app/features，shared 不引用业务层

2. 若升级 error 后出现历史违规（per D-10）：本 PR 内当场修掉或补豁免并说明理由，不留技术债

3. 重复直到 `npm run verify:governance` 退出码为 0
  </action>
  <verify>
    <automated>npm run verify:governance</automated>
  </verify>
  <acceptance_criteria>
    - npm run verify:governance 退出码为 0
    - 无新增 allowlist 豁免除非附有说明理由的注释
  </acceptance_criteria>
  <done>verify:governance 全绿</done>
</task>

</tasks>

<verification>
- npm run verify:governance 通过
- npm run verify:governance -- --dry-run 输出稳定
- 06-SMOKE-CHECKLIST 含删除/迁移路径项
- STRUCTURE.md 含 shared/utils 边界
</verification>

<success_criteria>
- CLN-01 达成：目录边界规则可执行，features/shared 已纳入 scope，三项规则升 error
- verify:governance 全绿可复现
</success_criteria>

<output>
After completion, create `.planning/phases/09-dir-boundary-cleanup/09-01-SUMMARY.md`
</output>
