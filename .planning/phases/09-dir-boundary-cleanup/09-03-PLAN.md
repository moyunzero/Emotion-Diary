---
phase: 09-dir-boundary-cleanup
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - utils/aiService.ts
  - components/EntryCard.tsx
  - components/Insights/EmotionReleaseArchive.tsx
  - components/ReviewExport/ReviewExportCanvas.tsx
  - components/Dashboard.tsx
  - services/companionDaysService.ts
  - utils/dateUtils.ts
  - __tests__/unit/utils/dateUtils.test.ts
  - __tests__/unit/utils/utilityFunctionTypes.test.ts
  - scripts/governance/allowlist.knip.json
autonomous: true
requirements: [CLN-02]
must_haves:
  truths:
    - "formatDateChinese、formatDateShort 调用点已迁移至 shared/formatting"
    - "dateUtils 中 deprecated 薄适配器已删除"
    - "formatDate、ensureMilliseconds 保留（若有调用）或迁至合理位置"
  artifacts:
    - path: shared/formatting
      provides: "formatDateChinese, formatMonthDay"
    - path: utils/dateUtils.ts
      provides: "formatDate, ensureMilliseconds（仅保留在用 API）"
  key_links:
    - from: components/EntryCard.tsx
      to: shared/formatting
      via: import formatDateChinese
      pattern: "from.*shared/formatting"
---

<objective>
迁移 dateUtils 的 formatDateChinese、formatDateShort（formatMonthDay）调用点到 @/shared/formatting，删除 deprecated 薄适配器；保留 formatDate、ensureMilliseconds（仍有调用则保留在 dateUtils 或迁至 shared）。同步收紧 allowlist。

Purpose: CLN-02 — 删除高置信死代码
Output: dateUtils deprecated 清零；调用点已迁移；allowlist 已收紧
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-dir-boundary-cleanup/09-RESEARCH.md
@utils/dateUtils.ts
@shared/formatting
</context>

<interfaces>
From shared/formatting:
- formatDateChinese(timestamp: number): string
- formatMonthDay(timestamp: number): string

From utils/dateUtils (保留):
- formatDate(timestamp: number): string
- ensureMilliseconds(timestamp: number): number
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate formatDateChinese and formatDateShort callers to shared/formatting</name>
  <files>utils/aiService.ts, components/EntryCard.tsx, components/Insights/EmotionReleaseArchive.tsx, components/ReviewExport/ReviewExportCanvas.tsx, components/Dashboard.tsx, services/companionDaysService.ts</files>
  <read_first>
    utils/dateUtils.ts
    shared/formatting/index.ts
    utils/aiService.ts
    components/EntryCard.tsx
    components/Insights/EmotionReleaseArchive.tsx
    components/ReviewExport/ReviewExportCanvas.tsx
    components/Dashboard.tsx
    services/companionDaysService.ts
  </read_first>
  <action>
按 RESEARCH 调用点清单迁移：

1. utils/aiService.ts：`import { formatDateChinese } from '../utils/dateUtils'` → `import { formatDateChinese } from '@/shared/formatting'`

2. components/EntryCard.tsx：`import { formatDateChinese } from ...` → `import { formatDateChinese } from '@/shared/formatting'`

3. components/Insights/EmotionReleaseArchive.tsx：formatDateChinese → `@/shared/formatting`

4. components/ReviewExport/ReviewExportCanvas.tsx：formatDateChinese → `@/shared/formatting`

5. components/Dashboard.tsx：formatDateChinese → `@/shared/formatting`

6. services/companionDaysService.ts：若有 formatDateChinese 或 formatDateShort/formatMonthDay，改为 `@/shared/formatting`；若无则跳过

注：formatDateShort 在 dateUtils 中映射到 formatMonthDay，迁移时使用 shared/formatting 的 formatMonthDay
  </action>
  <verify>
    <automated>rg "formatDateChinese|formatDateShort" utils/ components/ services/ --type-add 'ts:*.ts' --type-add 'tsx:*.tsx' -t ts -t tsx 2>/dev/null | grep -v "shared/formatting" | grep -v "dateUtils" || true</automated>
  </verify>
  <acceptance_criteria>
    - 上述 6 个文件中，对 formatDateChinese / formatMonthDay 的 import 均来自 @/shared/formatting
    - grep "from.*dateUtils" utils/aiService.ts components/EntryCard.tsx components/Insights/EmotionReleaseArchive.tsx components/ReviewExport/ReviewExportCanvas.tsx components/Dashboard.tsx services/companionDaysService.ts 无 formatDateChinese/formatDateShort 相关行
  </acceptance_criteria>
  <done>formatDateChinese/formatMonthDay 调用点已迁移至 shared/formatting</done>
</task>

<task type="auto">
  <name>Task 2: Remove deprecated from dateUtils and update tests</name>
  <files>utils/dateUtils.ts, __tests__/unit/utils/dateUtils.test.ts, __tests__/unit/utils/utilityFunctionTypes.test.ts, scripts/governance/allowlist.knip.json</files>
  <read_first>
    utils/dateUtils.ts
    __tests__/unit/utils/dateUtils.test.ts
    __tests__/unit/utils/utilityFunctionTypes.test.ts
    store/useAppStore.ts
  </read_first>
  <action>
1. 在 utils/dateUtils.ts 中删除 formatDateChinese 与 formatDateShort 的导出与实现（这两者为 deprecated 薄适配器）。保留 formatDate 与 ensureMilliseconds（store/useAppStore.ts 等仍使用 ensureMilliseconds）

2. 删除 dateUtils 顶部对 shared/formatting 的 import（formatDateChinese、formatMonthDay），因为不再 re-export

3. __tests__/unit/utils/dateUtils.test.ts：若测试 formatDateChinese/formatDateShort，改为从 @/shared/formatting 导入并断言，或删除这些用例（逻辑已在 shared 测试覆盖）

4. __tests__/unit/utils/utilityFunctionTypes.test.ts：若有 dateUtils.formatDateChinese 等引用，改为 shared/formatting 或移除

5. allowlist.knip.json：删除与 dateUtils deprecated 相关的 historicalDebt 条目（per D-09）

6. 运行 yarn test:unit __tests__/unit/utils/dateUtils.test.ts __tests__/unit/utils/utilityFunctionTypes.test.ts 与 npm run verify:governance
  </action>
  <verify>
    <automated>yarn test:unit __tests__/unit/utils/dateUtils.test.ts __tests__/unit/utils/utilityFunctionTypes.test.ts; npm run verify:governance</automated>
  </verify>
  <acceptance_criteria>
    - utils/dateUtils.ts 中不存在 formatDateChinese、formatDateShort 的 export 或实现
    - utils/dateUtils.ts 仍导出 formatDate 与 ensureMilliseconds
    - yarn test:unit __tests__/unit/utils/dateUtils.test.ts 退出码 0
    - npm run verify:governance 退出码 0
  </acceptance_criteria>
  <done>dateUtils deprecated 已删除；formatDate/ensureMilliseconds 保留；测试与治理通过</done>
</task>

</tasks>

<verification>
- dateUtils 无 formatDateChinese/formatDateShort
- 所有 formatDateChinese 调用来自 shared/formatting
- yarn test:unit + npm run verify:governance 通过
</verification>

<success_criteria>
- CLN-02 部分达成：dateUtils deprecated 已清零
</success_criteria>

<output>
After completion, create `.planning/phases/09-dir-boundary-cleanup/09-03-SUMMARY.md`
</output>
