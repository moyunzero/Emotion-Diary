---
phase: 09-dir-boundary-cleanup
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - utils/reviewStats.ts
  - utils/reviewExportClosingInput.ts
  - __tests__/unit/utils/reviewExportDerived.test.ts
  - __tests__/unit/utils/reviewExportClosingInput.test.ts
  - __tests__/unit/utils/reviewStatsTimeRange.test.ts
  - scripts/governance/allowlist.knip.json
autonomous: true
requirements: [CLN-02]
must_haves:
  truths:
    - "utils/reviewStatsTimeRange.ts 已删除"
    - "所有原 reviewStatsTimeRange 调用点已改为 @/shared/time-range"
    - "导出与洞察路径测试通过"
  artifacts:
    - path: shared/time-range
      provides: "getCalendarMonthRange, getReviewExportPeriods, ReviewExportPreset 等"
  key_links:
    - from: utils/reviewStats.ts
      to: shared/time-range
      via: import getCalendarMonthRange
      pattern: "from.*shared/time-range"
    - from: utils/reviewExportClosingInput.ts
      to: shared/time-range
      via: import ReviewExportPreset
      pattern: "from.*shared/time-range"
---

<objective>
删除 utils/reviewStatsTimeRange.ts 薄适配器，将所有调用点迁移到 @/shared/time-range，并收紧 allowlist。reviewStatsTimeRange 为纯 re-export，迁移成本最低（per D-05 批次顺序）。

Purpose: CLN-02 — 删除高置信死代码，保留删除证据与回滚点
Output: 已删除 utils/reviewStatsTimeRange.ts；调用点已迁移；allowlist 已收紧；SUMMARY 记录删除范围与回滚点
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-dir-boundary-cleanup/09-CONTEXT.md
@.planning/phases/09-dir-boundary-cleanup/09-RESEARCH.md
@utils/reviewStatsTimeRange.ts
@shared/time-range
@utils/reviewStats.ts
@utils/reviewExportClosingInput.ts
</context>

<interfaces>
From shared/time-range (canonical source):
- getCalendarMonthRange, getReviewExportPeriods, getMondayWeekRangeContaining, getPreviousCalendarMonthRangeBefore, getPreviousWeekRange
- REVIEW_PRESET_LABEL
- type ReviewExportPreset, type TimeRange
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all reviewStatsTimeRange imports to shared/time-range</name>
  <files>utils/reviewStats.ts, utils/reviewExportClosingInput.ts, __tests__/unit/utils/reviewExportDerived.test.ts, __tests__/unit/utils/reviewExportClosingInput.test.ts, __tests__/unit/utils/reviewStatsTimeRange.test.ts</files>
  <read_first>
    utils/reviewStats.ts
    utils/reviewExportClosingInput.ts
    __tests__/unit/utils/reviewExportDerived.test.ts
    __tests__/unit/utils/reviewExportClosingInput.test.ts
    __tests__/unit/utils/reviewStatsTimeRange.test.ts
    shared/time-range/index.ts
  </read_first>
  <action>
1. utils/reviewStats.ts：将 `import { getCalendarMonthRange } from './reviewStatsTimeRange'` 改为 `import { getCalendarMonthRange } from '@/shared/time-range'`

2. utils/reviewExportClosingInput.ts：将 `import type { ReviewExportPreset } from './reviewStatsTimeRange'` 改为 `import type { ReviewExportPreset } from '@/shared/time-range'`

3. __tests__/unit/utils/reviewExportDerived.test.ts：将 `import { getReviewExportPeriods } from '../../../utils/reviewStatsTimeRange'` 改为 `import { getReviewExportPeriods } from '@/shared/time-range'`

4. __tests__/unit/utils/reviewExportClosingInput.test.ts：将 `import { getReviewExportPeriods } from '../../../utils/reviewStatsTimeRange'` 改为 `import { getReviewExportPeriods } from '@/shared/time-range'`

5. __tests__/unit/utils/reviewStatsTimeRange.test.ts：将 `from '../../../utils/reviewStatsTimeRange'` 改为 `from '@/shared/time-range'`（测试的是 re-export 的同一实现，迁移后仍应通过）；若该测试文件仅验证薄适配器转调，可考虑将用例并入 __tests__/unit/shared/time-range 已有测试，或保留并仅改 import
  </action>
  <verify>
    <automated>rg "reviewStatsTimeRange" --type-add 'ts:*.ts' --type-add 'tsx:*.tsx' -t ts -t tsx app components store utils hooks services lib features shared 2>/dev/null; test $? -ne 0 || true</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "reviewStatsTimeRange" utils/ components/ app/ store/ services/ 无命中（排除 .planning 与 __tests__ 中注释）
    - utils/reviewStats.ts 包含 "from '@/shared/time-range'" 或 "from \"@/shared/time-range\""
    - utils/reviewExportClosingInput.ts 包含 "from '@/shared/time-range'"
    - __tests__/unit/utils/reviewExportDerived.test.ts 与 reviewExportClosingInput.test.ts 的 import 指向 shared/time-range
  </acceptance_criteria>
  <done>所有 reviewStatsTimeRange 调用点已迁移至 shared/time-range</done>
</task>

<task type="auto">
  <name>Task 2: Delete utils/reviewStatsTimeRange.ts and tighten allowlist</name>
  <files>utils/reviewStatsTimeRange.ts, scripts/governance/allowlist.knip.json</files>
  <read_first>
    utils/reviewStatsTimeRange.ts
    scripts/governance/allowlist.knip.json
  </read_first>
  <action>
1. 删除 utils/reviewStatsTimeRange.ts

2. 在 scripts/governance/allowlist.knip.json 中：若有与 reviewStatsTimeRange 相关的 historicalDebt.unusedExports 或 unusedDependencies 条目，删除该条目（per D-09）

3. 运行 `yarn test:unit __tests__/unit/utils/reviewExportDerived.test.ts __tests__/unit/utils/reviewExportClosingInput.test.ts __tests__/unit/utils/reviewStatsTimeRange.test.ts` 确认通过（若 reviewStatsTimeRange.test.ts 已改为从 shared 导入，则仍应通过；若已合并到 shared 测试则按实际路径跑）

4. 运行 `npm run verify:governance` 确认全绿
  </action>
  <verify>
    <automated>test ! -f utils/reviewStatsTimeRange.ts && yarn test:unit __tests__/unit/utils/reviewExportDerived.test.ts __tests__/unit/utils/reviewExportClosingInput.test.ts 2>/dev/null; npm run verify:governance</automated>
  </verify>
  <acceptance_criteria>
    - utils/reviewStatsTimeRange.ts 不存在
    - yarn test:unit __tests__/unit/utils/reviewExportDerived.test.ts __tests__/unit/utils/reviewExportClosingInput.test.ts 退出码 0
    - npm run verify:governance 退出码 0
    - allowlist.knip.json 中无 reviewStatsTimeRange 相关残留条目
  </acceptance_criteria>
  <done>reviewStatsTimeRange 已删除；测试与治理通过</done>
</task>

</tasks>

<verification>
- utils/reviewStatsTimeRange.ts 已删除
- grep -r "reviewStatsTimeRange" utils/ 无命中
- yarn test:unit（export 相关）通过
- npm run verify:governance 通过
</verification>

<success_criteria>
- CLN-02 部分达成：reviewStatsTimeRange deprecated 已清零
- 删除证据记录于 09-02-SUMMARY.md
</success_criteria>

<output>
After completion, create `.planning/phases/09-dir-boundary-cleanup/09-02-SUMMARY.md`（含删除文件列表、回滚点 commit）
</output>
