---
phase: 10-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - __tests__/unit/utils/example.test.ts
  - __tests__/property/example.property.test.ts
  - __tests__/unit/utils/reviewStatsTimeRange.test.ts
autonomous: true
requirements: [TST-01]
must_haves:
  truths:
    - "示例 Jest / fast-check 脚手架测试文件已从仓库移除"
    - "与 shared/time-range 重复的 utils 侧测试已移除，canonical 单测仍覆盖导出周期语义"
    - "yarn test:ci 与 verify-governance-smoke 在清理后退出码为 0"
  artifacts:
    - path: scripts/verify-governance-smoke.js
      provides: "SMOKE_PATHS 仍指向存在的测试文件（本计划不修改该脚本）"
  key_links:
    - from: "__tests__/unit/shared/time-range/periods.test.ts"
      to: "@/shared/time-range"
      via: "import getReviewExportPeriods / REVIEW_PRESET_LABEL"
      pattern: "getReviewExportPeriods|REVIEW_PRESET_LABEL"
---

<objective>
按 Phase 10 CONTEXT **D-01、D-03** 删除示例测试与重复的 time-range 单测；用全量 CI Jest 与治理 smoke 证明关键路径（记录 / 导出 / 同步）测试集仍稳定（与 Phase 6 **D-01/D-15** 一致：lint+测试+smoke 组合在后续计划中收口至 GHA）。

Purpose: **TST-01** — 删除示例/低价值/重复测试中已锁定部分，并验证套件与 smoke 仍绿  
Output: 删除 3 个测试文件；本地/CI 可复现的 `yarn test:ci` + `node scripts/verify-governance-smoke.js` 通过
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-ci/10-CONTEXT.md
@.planning/phases/10-ci/10-RESEARCH.md
@.planning/phases/06-governance-baseline-gates/06-CONTEXT.md
@scripts/verify-governance-smoke.js
@__tests__/unit/shared/time-range/periods.test.ts
@__tests__/unit/utils/reviewStatsTimeRange.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 删除示例测试（D-01）</name>
  <files>__tests__/unit/utils/example.test.ts, __tests__/property/example.property.test.ts</files>
  <read_first>
    __tests__/unit/utils/example.test.ts
    __tests__/property/example.property.test.ts
    .planning/phases/10-ci/10-CONTEXT.md
  </read_first>
  <action>
1. 删除文件 `__tests__/unit/utils/example.test.ts`（全文以「示例测试文件」「测试基础设施验证」为描述，仅验证 Jest 基础能力，无业务断言；per **D-01**）。
2. 删除文件 `__tests__/property/example.property.test.ts`（全文以「示例属性测试文件」「属性测试基础设施验证」为描述，仅验证 fast-check 基础能力；per **D-01**）。
3. 不新增替代「基础设施冒烟」测试文件（用户明确不保留单独冒烟测试）。
  </action>
  <verify>
    <automated>test ! -f __tests__/unit/utils/example.test.ts && test ! -f __tests__/property/example.property.test.ts && yarn test:ci</automated>
  </verify>
  <acceptance_criteria>
    - 路径 `__tests__/unit/utils/example.test.ts` 在仓库中不存在（`test ! -f` 为真）
    - 路径 `__tests__/property/example.property.test.ts` 在仓库中不存在（`test ! -f` 为真）
    - 命令 `yarn test:ci` 退出码为 0
  </acceptance_criteria>
  <done>两处示例测试文件已删除且 test:ci 仍通过</done>
</task>

<task type="auto">
  <name>Task 2: 删除与 shared/time-range 重复的 utils 单测（D-03）</name>
  <files>__tests__/unit/utils/reviewStatsTimeRange.test.ts</files>
  <read_first>
    __tests__/unit/utils/reviewStatsTimeRange.test.ts
    __tests__/unit/shared/time-range/periods.test.ts
    .planning/phases/10-ci/10-CONTEXT.md
  </read_first>
  <action>
1. 打开 `__tests__/unit/utils/reviewStatsTimeRange.test.ts`：确认其 import 源仅为 `@/shared/time-range`（无 `utils/reviewStatsTimeRange` 实现文件被测）。
2. 打开 `__tests__/unit/shared/time-range/periods.test.ts`：确认存在对 `REVIEW_PRESET_LABEL` 的断言，且存在对 `getReviewExportPeriods` 的用例（含固定日期 `2026-2-21` 的周/月语义或等价边界断言）。
3. 若第 2 步已覆盖 `reviewStatsTimeRange.test.ts` 中对 `getReviewExportPeriods` / `getCalendarMonthRange` / preset 标签的语义，则 **删除** `__tests__/unit/utils/reviewStatsTimeRange.test.ts` 整文件（保留「最贴近源码」的 `__tests__/unit/shared/time-range/periods.test.ts`，per **D-03**）。
4. 若发现 `reviewStatsTimeRange.test.ts` 中有 **periods.test.ts 未覆盖** 的独立 `it(...)` 断言，则将缺失用例 **迁移** 到 `periods.test.ts` 后再删除原文件（禁止静默丢断言）。
  </action>
  <verify>
    <automated>test ! -f __tests__/unit/utils/reviewStatsTimeRange.test.ts && yarn test:ci</automated>
  </verify>
  <acceptance_criteria>
    - `__tests__/unit/utils/reviewStatsTimeRange.test.ts` 不存在
    - `__tests__/unit/shared/time-range/periods.test.ts` 文件内容包含字符串 `getReviewExportPeriods` 与 `REVIEW_PRESET_LABEL`（`rg -n` 可命中）
    - `yarn test:ci` 退出码为 0
  </acceptance_criteria>
  <done>重复 time-range 单测已移除，canonical 单测仍覆盖核心语义</done>
</task>

<task type="auto">
  <name>Task 3: 关键路径 smoke 回归（D-15 门槛对齐）</name>
  <files>（无代码变更，仅执行命令）</files>
  <read_first>
    scripts/verify-governance-smoke.js
    .planning/phases/06-governance-baseline-gates/06-SMOKE-CHECKLIST.md
  </read_first>
  <action>
1. 执行 `node scripts/verify-governance-smoke.js`（非 dry-run），确认三条路径 `record` / `export` / `sync` 均执行且进程退出码为 0。
2. 控制台摘要须出现 `record`、`export`、`sync` 字样及 `Structured assertions only for export path`（与现有脚本 `printSummary` 行为一致）。
3. 若 smoke 因「找不到测试文件」失败：停止并修复 **SMOKE_PATHS** 或恢复被误删路径（本计划预期不移动 smoke 目标文件）。
  </action>
  <verify>
    <automated>node scripts/verify-governance-smoke.js</automated>
  </verify>
  <acceptance_criteria>
    - 命令 `node scripts/verify-governance-smoke.js` 退出码为 0
    - 该命令标准输出中包含子串 `record`、`export`、`sync`（顺序不限）
    - `scripts/verify-governance-smoke.js` 内 `SMOKE_PATHS` 仍包含字符串 `__tests__/unit/store/storage.test.ts`、`__tests__/unit/utils/reviewExportDerived.test.ts`、`__tests__/unit/store/syncStatus.test.ts`（`rg` 可命中）
  </acceptance_criteria>
  <done>清理后治理 smoke 全绿，关键路径自动化仍可达</done>
</task>

</tasks>

<verification>
- `yarn test:ci` 通过
- `node scripts/verify-governance-smoke.js` 通过
</verification>

<success_criteria>
- 示例测试文件已删除（D-01）
- `reviewStatsTimeRange.test.ts` 已删除且 shared 侧单测仍覆盖 time-range 语义（D-03）
- 全量 CI Jest 与治理 smoke 无回归
</success_criteria>

<output>
After completion, create `.planning/phases/10-ci/10-01-SUMMARY.md`
</output>
