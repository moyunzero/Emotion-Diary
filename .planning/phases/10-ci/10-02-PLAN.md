---
phase: 10-ci
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - __tests__/unit/features/profile.test.tsx
  - __tests__/unit/shared/formatting/ensureMilliseconds.test.ts
  - __tests__/README.md
  - jest.config.js
autonomous: true
requirements: [TST-02]
must_haves:
  truths:
    - "features 源码对应的单测位于 __tests__/unit/features，而非误放在 app/"
    - "仅测 shared/formatting.ensureMilliseconds 的文件路径落在 __tests__/unit/shared/formatting/"
    - "README 列出与源码顶层目录一致的 unit 子树及映射表"
    - "jest.config.js 的 collectCoverageFrom 包含 features/ 与 shared/"
  artifacts:
    - path: __tests__/unit/features/profile.test.tsx
      provides: "Profile 路由/特性相关单测归属 features"
    - path: __tests__/unit/shared/formatting/ensureMilliseconds.test.ts
      provides: "ensureMilliseconds 与 shared/formatting 对齐"
    - path: __tests__/README.md
      provides: "目录树 + 源码→测试根映射"
    - path: jest.config.js
      provides: "coverage 收集含 features、shared"
  key_links:
    - from: "features/"
      to: "__tests__/unit/features/"
      via: "README 映射表与物理路径"
      pattern: "features/"
---

<objective>
将测试文件路径与 **STRUCTURE.md** 中的源码边界对齐：新增 `__tests__/unit/features`，迁移 Profile 单测；将仅测 `shared/formatting` 的文件移入 `__tests__/unit/shared/formatting/`；更新 `__tests__/README.md` 与 `jest.config.js` 的 `collectCoverageFrom`（与 10-RESEARCH Pitfall 4 一致）。

Purpose: **TST-02** — 测试目录与 feature/store/shared/integration 心智模型一致，失败时快速定位责任模块  
Output: 迁移后的测试路径、文档化映射、覆盖率收集与目录一致
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/STRUCTURE.md
@.planning/phases/10-ci/10-CONTEXT.md
@.planning/phases/10-ci/10-RESEARCH.md
@jest.config.js
@__tests__/README.md
@__tests__/unit/app/profile.test.tsx
@__tests__/unit/utils/dateUtils.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 unit/features 并迁移 Profile 单测</name>
  <files>__tests__/unit/features/profile.test.tsx</files>
  <read_first>
    __tests__/unit/app/profile.test.tsx
    .planning/codebase/STRUCTURE.md
    app/profile.tsx
  </read_first>
  <action>
1. 创建目录 `__tests__/unit/features/`（若不存在）。
2. 使用 `git mv` 将 `__tests__/unit/app/profile.test.tsx` 移至 `__tests__/unit/features/profile.test.tsx`（保留历史）。
3. 打开迁移后的文件，将所有相对 import 从「`__tests__/unit/app` 深度」校准为「`__tests__/unit/features` 深度」：例如原 `../../../app/profile` 在 `unit/app` 下指向根目录 app；在 `unit/features` 下同样为 **三层** 回到仓库根（`../../../app/profile`），若 `tsc`/Jest 报错则改为正确的 `../../../app/profile` 或 `@/app/profile`（与仓库别名一致即可）。
4. 不得删除 `__tests__/unit/app/` 目录本身：该目录仍存在 `layoutCleanup.test.tsx` 等其他单测，仅迁移 `profile.test.tsx`。
  </action>
  <verify>
    <automated>test -f __tests__/unit/features/profile.test.tsx && test ! -f __tests__/unit/app/profile.test.tsx && yarn test:ci -- __tests__/unit/features/profile.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - 文件 `__tests__/unit/features/profile.test.tsx` 存在
    - 文件 `__tests__/unit/app/profile.test.tsx` 不存在
    - `yarn test:ci -- __tests__/unit/features/profile.test.tsx` 退出码为 0
  </acceptance_criteria>
  <done>Profile 单测已归属 features 桶</done>
</task>

<task type="auto">
  <name>Task 2: 迁移 ensureMilliseconds 单测至 shared/formatting</name>
  <files>__tests__/unit/shared/formatting/ensureMilliseconds.test.ts</files>
  <read_first>
    __tests__/unit/utils/dateUtils.test.ts
    __tests__/unit/shared/formatting/date.test.ts
    shared/formatting/index.ts
  </read_first>
  <action>
1. 使用 `git mv` 将 `__tests__/unit/utils/dateUtils.test.ts` 移至 `__tests__/unit/shared/formatting/ensureMilliseconds.test.ts`。
2. 将文件顶注释中的「日期工具函数测试」改为明确说明：本文件仅覆盖 `@/shared/formatting` 的 `ensureMilliseconds`（与实现位置一致）。
3. 将最外层 `describe('dateUtils'` **重命名**为 `describe('shared/formatting ensureMilliseconds'`（精确字符串，便于检索）。
4. 保持所有 `import { ensureMilliseconds } from '@/shared/formatting';` 或相对路径 import 与 `date.test.ts` 风格一致（推荐 `@/shared/formatting`）；修正若因路径深度变化导致的相对 import 错误。
  </action>
  <verify>
    <automated>test -f __tests__/unit/shared/formatting/ensureMilliseconds.test.ts && test ! -f __tests__/unit/utils/dateUtils.test.ts && yarn test:ci -- __tests__/unit/shared/formatting/ensureMilliseconds.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `__tests__/unit/shared/formatting/ensureMilliseconds.test.ts` 存在
    - `__tests__/unit/utils/dateUtils.test.ts` 不存在
    - 文件 `ensureMilliseconds.test.ts` 含字面量 `describe('shared/formatting ensureMilliseconds'`
    - `yarn test:ci -- __tests__/unit/shared/formatting/ensureMilliseconds.test.ts` 退出码为 0
  </acceptance_criteria>
  <done>ensureMilliseconds 测试路径与 shared/formatting 对齐</done>
</task>

<task type="auto">
  <name>Task 3: 更新 README 目录树与 jest collectCoverageFrom</name>
  <files>__tests__/README.md, jest.config.js</files>
  <read_first>
    __tests__/README.md
    jest.config.js
    jest.ci.config.js
  </read_first>
  <action>
1. 重写 `__tests__/README.md` 的「目录结构」代码块，使其 **真实反映** 当前 `__tests__/unit/` 子目录，至少显式列出：`app/`、`components/`、`features/`、`hooks/`、`services/`、`shared/`、`store/`、`utils/`、`scripts/`（若某目录不存在则不要写进树；执行时以 `ls __tests__/unit` 为准更新）。
2. 在 README 中新增二级标题 `## 源码目录 → 单测根目录映射`，用 Markdown 表格列出至少以下行（路径字面量须一致）：
   - `app/` → `__tests__/unit/app/`
   - `features/` → `__tests__/unit/features/`
   - `shared/` → `__tests__/unit/shared/`
   - `store/` → `__tests__/unit/store/`
   - `utils/` → `__tests__/unit/utils/`
   - `components/` → `__tests__/unit/components/`
   - `hooks/` → `__tests__/unit/hooks/`
   - `services/` → `__tests__/unit/services/`
   - `scripts/`（指仓库根 `scripts/`）→ `__tests__/unit/scripts/`
3. 在 `jest.config.js` 的 `collectCoverageFrom` 数组中，在现有 `hooks/**/*` 项之后 **按字母顺序** 插入两行字符串：`"features/**/*.{ts,tsx}",` 与 `"shared/**/*.{ts,tsx}",`（不删除现有条目；保持 JSON 语法合法）。
  </action>
  <verify>
    <automated>yarn test:ci</automated>
  </verify>
  <acceptance_criteria>
    - `__tests__/README.md` 包含二级标题行 `## 源码目录 → 单测根目录映射`
    - `__tests__/README.md` 表格中含子串 `features/`、`__tests__/unit/features/`
    - `jest.config.js` 中 `collectCoverageFrom` 数组同时包含字面量 `"features/**/*.{ts,tsx}"` 与 `"shared/**/*.{ts,tsx}"`
    - `yarn test:ci` 退出码为 0
  </acceptance_criteria>
  <done>文档与覆盖率收集与源码边界一致</done>
</task>

</tasks>

<verification>
- `yarn test:ci` 全量通过
- `node scripts/verify-governance-smoke.js` 仍为 0（未改 smoke 列表时须保持）
</verification>

<success_criteria>
- `__tests__/unit/features/` 存在且含 profile 单测
- `dateUtils.test.ts` 已更名为 shared/formatting 下的 ensureMilliseconds 单测
- README 与 jest coverage 配置反映 features/shared
</success_criteria>

<output>
After completion, create `.planning/phases/10-ci/10-02-SUMMARY.md`
</output>
