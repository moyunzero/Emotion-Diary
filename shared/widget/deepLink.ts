/**
 * Fixed widget tap deep link — Dashboard / main tabs only (PLT-02 / D-06).
 */

/** Scheme root with three slashes → Expo Router tabs Dashboard. */
export const WIDGET_DEEP_LINK_URL = 'emotiondiary:///' as const;
